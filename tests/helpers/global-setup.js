/**
 * Jest global setup for integration tests.
 * Currently a no-op placeholder. Real integration tests would
 * start the database / seed data here.
 */
module.exports = async function globalSetup() {
  // No-op for now
};
