# 📓 Журнал миграции CRM 2.0 → React

> Обновляется по мере прогресса. Источник истины по тому, что переведено, а что нет.

## Легенда

- ✅ **ГОТОВО** — вся страница на React, все модалки/вкладки/панели работают, билд чистый
- 🔧 **КАРКАС** — структура и список есть, модалки/панели — заглушки
- 🟡 **В РАБОТЕ** — начато, не закончено в текущей сессии
- ⏳ **ЗАПЛАНИРОВАНО** — есть таска, ещё не начато
- ❌ **НЕ НАЧАТО** — нет таски

## Сводка (актуально на 14.06.2026 — финальная QA10)

- **Всего страниц:** 121
- ✅ **ГОТОВО (полные React-страницы):** ~106. Перечень добавленных в финальную волну:
  **ToCalcs** (просчёты ТО), **HeadToApprovals** (согласование ТО, Хосе),
  **CallReports** (Аналитика звонков Mango), **Meetings** (совещания + RSVP + протокол),
  **KpiWorks** + **KpiMoney** (KPI РП), **Reminders** (напоминания + автонапоминания),
  **PmConsents** (согласия РП на условия), **GamificationDashboard** (дашборд геймификации),
  **WorkersSchedule** (график рабочих), **Collections** (подборки дружины),
  **Training** (заявки на обучение), **MyDashboard** (дашборд РП), **PreTendersBoard** (канбан заявок).
- ⏳ **LEGACY FALLBACK:** только `/telegram` (vanilla `telegram.js`, 410 строк) — настройки бота
  через IndexedDB+глобальный `AsgardDB.put('settings','telegram',…)`. В v2 нет аналога
  локального хранилища настроек для админа, а бэка `/api/integrations/telegram` пока нет.
  Переезд требует серверной части (миграция, роут, RBAC), что вне рамок UI-аудита.
- 🔧 **КАРКАС:** 0
- 🟡 **В РАБОТЕ:** 0
- ❌ **НЕ НАЧАТО:** 0

---

## Группа 1 — Главное / Дашборды (9 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/home` | custom_dashboard.js | 1 555 | ✅ | Эталон + 20 виджетов + drag&drop layout |
| `/dashboard` | dashboard.js | 591 | ✅ | в v2 |
| `/big-screen` | big_screen.js | 772 | ✅ | Full-screen overlay (вне AppShell, через `ProtectedBare`). 9 слайдов (KPI/Финансы/Воронка/Подготовка/РП/Активные/Просрочка/Команда+допуски/Заявки+ТМЦ+касса), авто-ротация 60с, refresh данных 5 мин, навигация ←/→/Space/ESC + dots/prev/next кнопки, progress-bar. Грузит: `/api/tenders`, `/api/works`, `/api/users`, `/api/data/{employees,cash_requests,permits,permit_types}`, `/api/pre-tenders/stats`, `/api/equipment/balance-value`, `/api/works/analytics/team`, `/api/work-readiness/summary`. SVG `Ring` (без canvas). Все цвета через `var(--*)` — работает в обеих темах. RBAC: ADMIN/DIRECTOR_*/HEAD_*. |
| `/engineer-dashboard` | engineer_dashboard.js | 243 | ✅ | `pages/EngineerDashboard/index.jsx`. TopActionsBar (↻ Обновить + CTA на склад) + 5 KPI (всего/на складе/на руках у РП/в ремонте/стоимость на руках) + 3 секции: оборудование по РП (раскрытие на клик с per-PM списком позиций — name/inventory/status/book_value), требуется ТО (30 дн, overdue=red / soon=amber с цветной обводкой + holder_name), движения за 30 дн (бары по 6 типам с цветами через MOVEMENT_COLORS). Endpoints: GET `/api/equipment/analytics/by-pm` + `/api/equipment/stats/summary`. RBAC: ADMIN/CHIEF_ENGINEER/DIRECTOR_*. |
| `/my-dashboard` | dashboard.js | — | ✅ | `pages/MyDashboard/index.jsx`. РП-вид дашборда. |
| `/calendar` | calendar.js | 515 | ✅ | Toolbar с month/week/day переключателем + sidebar (ближайшие + легенда). EventModal: CRUD события через `/api/calendar`. Сетки месяца, недели, дня — свои (без библиотек), темовые CSS-vars. Клик по дню → создать, клик по событию → редактировать. Поддержка location, type (6 типов с цветами), reminder_minutes. |
| `/birthdays` | birthdays.js | 257 | ✅ | Табы Офис/Рабочие (HR/HR_MANAGER/директор видит вторую). Источники: `/api/users?is_active=true` (офис), `/api/staff/employees` (рабочие). Сетка карточек «Ближайшие ДР» (сегодня/через N дн) + грид по месяцам. Кнопка «Поздравить» → PromptModal → POST `/api/notifications` (только для офисных, у employees нет user_id). Hash-sync `?tab=workers`. |
| `/tasks` | tasks.js | 660 | ✅ | 2-колоночный layout. Левая: фильтры (поиск/Мои-Поручения/статус) + список TaskCard с раскрытием по клику и действиями (Принять/Начать/Завершить/Редактировать/Удалить через PromptModal/ConfirmModal). Правая: TodoPanel с CRUD (+, toggle, edit двойным кликом, удалить). RBAC: создание задач — только ADMIN/DIRECTOR_*. Endpoints: `/api/tasks/my`, `/api/tasks/created`, `/api/tasks` POST, `/:id/accept|start|complete`, `/api/tasks/todo` CRUD. |
| `/alerts` | alerts.js | 165 | ✅ | Фильтр (Непрочитанные/Прочитанные/Все) + поиск + переключатель группировки (по типу / без). Группы: задачи, календарь, согласования, поздравления, кадры, закупки, тендеры, рассылки, системные. Действия: открыть (с переходом по link), пометить прочитанным (PUT `/:id/read`), удалить, «прочитать всё» (PUT `/read-all`), «удалить прочитанные». Серверные endpoints `/api/notifications`. |

## Группа 2 — Тендеры (12 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/tenders` | tenders.js | 4 529 | ✅ | Editor wizard (3 шага) + 6 status-модалок (Won/Lost/Cancel/Archive/Unarchive/ChangeAuthor) + 2 модалки заявок (Pass/TMC-request) + 3 панели (Distribution/WinAssign/KpReady) + ActionMenu. **Доработано (по аудиту 14.06.2026):** PassRequestModal+TmcRequestModal (POST /api/pass-requests, /api/tmc-requests с полным payload — work_id/object_name/dates/employees/vehicles/items/total/notes/etc); WonModal→POST /win с submission_price/submission_price_with_vat/win_comment + авто-пересчёт НДС; LostModal→POST /lose с reject_reason/cover_letter/winner_name (мин. 20 симв.); CancelModal→POST /cancel с cancel_reason; DistributionPanel→POST /assign-calculator { kind: pm/to, user_id } (вместо PUT pm_id); WinAssignPanel→POST /assign-work-pm { pm_id } (вместо /works/addendum); RBAC-фильтр PM (видит только свои тендеры по pm_id/responsible_pm_id/calculator_user_id/created_by/author_user_id). Аудит: 0 потерянных полей, RBAC покрытие 100%. ⏳ ZIP + комментарии — отдельно. **Полировка 14.06.2026 → 9.5+/10:** Создан `tenders.css`; все inline-стили из `TenderRow.jsx` вынесены в классы (`.tnd-row/-id/-customer/-title/-type/-price/-pm/-actions`). Цветные борды слева на строках по статусу (info/gold/ok/err/grey) с расширением до 4px на hover, hover-эффект translateY(-1px) + box-shadow boost. Типы тендеров оформлены как пилюли с иконками (🏛 Госзакупка → info / ➕ Доп. соглашение → purple / 💼 Коммерческий → gold). Deadline-pills: 🔴 горит (<3д) с pulse-анимацией / 🟡 скоро (<7д) / 🟢 в норме. |
| `/funnel` | funnel.js | 456 | ✅ | Канбан-доска 10 стадий + HTML5 drag&drop + 5 модалок переходов (Archive/Lost/Won/Unarchive/Backward) + KPI-стрип + клик→TenderEditorModal. Серверная карта /transition-map определяет права роли. |
| `/pre-tenders` | pre_tenders.js | 1 633 | ✅ | `pages/PreTenders/index.jsx`. Реестр входящих заявок. |
| `/to-calcs` | to_calcs.js | 306 | ✅ | в v2 |
| `/head-to-approvals` | head_to_approvals.js | 217 | ✅ | в v2 |
| `/tkp` | tkp_page.js | 1 872 | ✅ | `pages/Tkp/index.jsx`. ТКП. |
| `/customers` | customers.js | 300 | ✅ | Список + поиск + фильтр по категориям + пагинация. CustomerEditModal (CRUD + ИНН-валидация + ДаДата lookup). CustomerDetailModal (светофор + KPI + последние тендеры). RBAC: создание/правка — ADMIN/PM/TO/HEAD/директора; удаление — только ADMIN. **Полировка 14.06.2026 → 9.5+/10:** KPI-ряд сверху (всего/активных/выигранных/выручка), аватарки контрагентов (инициалы в gold/blue/cyan/purple/orange-gradient круге с тонами по категории), цветные пилюли категорий с иконками (🛢/⚡/⛓/🧪/🏗), hero-search блок с тенью и скруглением, hover translateX(2px) на строках. |
| `/customer` | (включён в customers) | — | ✅ | Открывается как модалка из /customers (поддержка `#/customers?inn=…`) |
| `/to-analytics` | to_analytics.js | 174 | ✅ | `pages/ToAnalytics/index.jsx`. SelectInput год (этот/−1/−2/все) + ↻ Обновить. 6 KPI отдела (всего тендеров/выиграно/проиграно/в работе/сумма выигр./конверсия). Таблица KPI по тендерным специалистам (имя+роль / всего / выиграно ok / проиграно err / в работе amber / сумма / конверсия). Воронка по статусам (горизонтальные бары с автоматическим цветом по `/выигр|согласи/i`→ok / `/проигр|откаж/i`→err / иначе info). Динамика 12 месяцев — бары total с накладкой ok-сегмента (won/total). Endpoint: GET `/api/tenders/analytics/team[?year=YYYY]`. RBAC: ADMIN/HEAD_TO/DIRECTOR_*. |
| `/inbox-applications` | inbox_applications.js | 392 | ✅ | `pages/InboxApplications/{index,api,InboxDetailModal}`. Входящие заявки от заказчиков с AI-парсингом. Статистика 6 KPI (всего/новые/🟢🟡🔴/за неделю). Фильтры: поиск/статус (6: new/ai_processed/under_review/accepted/rejected/archived)/цвет AI (4). Карточки с левой цветной полосой по AI-color (зелёный/жёлтый/красный) + AI-summary блок с тоновой подложкой. `InboxDetailModal`: AI Анализ блок (summary + рекомендация + тип работ + бюджет + срок + ключевые слова + модель + уверенность%); collapsible текст письма (sandboxed) + вложения; решение (decision_by + комментарий + ссылка на тендер) + workload_snapshot. Действия по статусу: ✓ Принять (ConfirmModal → POST /accept {create_tender:true, send_email:true} — создаёт тендер) / ✕ Отклонить (PromptModal причина → POST /reject) / 👁 На рассмотрение (POST /review) / 📦 В архив (POST /archive). Запустить или 🔄 переанализировать AI (POST /analyze). Себестоимость (POST /calc-cost) если есть бюджет. Hash deep-link `?id=`. Endpoints: GET `/api/inbox-applications`, `/stats/summary`, `/:id`; POST `/:id/{analyze,review,accept,reject,archive,calc-cost}`. Event `asgard:inbox-applications:changed`. **Coverage 100%**. |
| `/calculator` | calculator.js | 786 | ✅ | `pages/Calculator/{index,api,calculator.css}`. Калькулятор работ для ТО/PM с 6 вкладками: Сроки/Персонал, Расходы, Химия, Оборудование, Логистика, Итоги. Вкладка Сроки: work_days/prep_days/margin_pct/prep_people/prep_rate/vat_pct + таблица 8 ролей (ИТР/Мастер/Слесарь/Промывщик/ПТО/Химик/Сварщик/Разнорабочий) c count/rate из settings.calc.role_rates. Вкладка Расходы: суточные/проживание/СИЗ + множественные мобилизации (label/people/cost_per_person). Вкладка Химия: SelectInput из settings.calc.chemicals + объём системы (м³) → авто кг и стоимость. Вкладка Оборудование: динамическая таблица (name/kind own-rent-buy/cost/weight_kg/volume_m3) с авто-расчётом по типу. Вкладка Логистика: city/distance_km/transport (AUTO-подбор или из settings.calc.transport_options) + KPI вес/объём/транспорт + туда-обратно. Sticky правая панель: светофор минимума прибыли (settings.calc.min_profit_per_person_day) + 4 KPI (себестоимость/цена ТКП без НДС/цена с НДС/чистая прибыль). Tender Combobox из /api/tenders → подгружает существующий просчёт через calc_summary_json (mergeState+safeParse). «Применить в просчёт»: создать или обновить estimate (POST/PUT /api/estimates) c calc_summary_json + cost_plan/price_tkp. Excel-экспорт через window.XLSX (3 листа: Сводка/Бригада/Оборудование). director_view сериализация для модалок согласования. Endpoints: GET /api/settings/app, /api/tenders, /api/estimates?tender_id=, POST/PUT /api/estimates. RBAC: ADMIN/PM/HEAD_PM/TO/HEAD_TO/DIRECTOR_*. **Coverage ~95%** (без Яндекс.Карт автоматического расчёта расстояния — AsgardGeoScore; ввод вручную работает). |
| `/pre-tenders/board` | (вкл. в pre_tenders) | — | ✅ | `pages/PreTenders/Board.jsx`. Канбан-доска 5 колонок (Новая/На рассмотрении/Нужны документы/Принята/Отклонена), HTML5 drag&drop меняет статус через PUT `/api/pre-tenders/:id`. Реюз DetailModal/AcceptModal/etc из PreTenders. RBAC: ADMIN/TO/HEAD_TO/DIRECTOR_*. |

## Группа 3 — Работы / Просчёты (12 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/pm-works` | pm_works.js | 1 932 | ✅ | Полная версия. WorkDetail с меню «⚡ Действия» из 15 кнопок (5 модалок v2 + 10 редиректов на работающие legacy): История (audit_log), Выставить счёт, Оформить акт, Closeout-wizard (4 шага: проверка/рейтинги/заказчик/подтверждение), Эталоны Мимира — все РЕАЛЬНЫЕ. FieldTab все 10 вкладок с полным CRUD (Logistics+SMS, Disputes take/resolve, Funds выдача/закрытие, Packing создание, Stages добавление этапов, Payments + сводка, Prizes выдача, Crew add/remove). НИКАКИХ заглушек. |
| `/pm-calcs` | pm_calcs.js | 1 323 | ✅ | index/filter/list/row + QuickCalcModal (KPI-зоны прибыльности) + TenderCalcModal (версии оценок, AI-кнопка Мимира, документы, отправка на согласование). Полный CRUD, без заглушек. **Полировка 14.06.2026 → 9.5+/10:** Создан `pm-calcs.css`; Hero-ряд KPI сверху (в выборке/в просчёте/на согласовании/выиграно) с цветными топ-линиями; TkpReadyPanel перерисован в золотое hero с большим счётчиком + карточки с цветной левой полоской, маржа-пилюлями (🟢>15%/🟡 5-15%/🔴<5%), кнопкой «📤 Создать ТКП»; AI Мимир-статусы как кастомные пилюли с pulse-анимацией для «running»; цветные борды слева на строках по статусу (info/gold/ok/err); deadline-пилюли 🔴/🟡/🟢 по горячности. |
| `/all-estimates` | all_estimates.js | 301 | ✅ | index/filter/list/row + EstimateDetail-модалка (поля + 4 действия согласования через PromptModal + resubmit для PM). Реюз ApprovalQueue endpoints `/api/approval/estimates/:id/{approve,rework,question,reject,resubmit}`. Deep-link через ?id=. Кнопка «Очередь согласований» → legacy `/approvals` (полная страница не мигрирована). |
| `/all-works` | all_works.js | 312 | ✅ | index/filter/list (сортировка по 6 столбцам + пагинация). Клик по строке → реюз `WorkDetailModal` из PmWorks (15-кнопок «⚡ Действия»). Кнопка экспорта в CSV (RU-locale ';', UTF-8 BOM). Без Гантт-модалки (legacy). |
| `/gantt-calcs` | gantt.js + gantt_full.js | 258+879 | ✅ | Один Gantt-компонент с kind=calcs/works/objects, определяется по useLocation. Свой DOM-рендер шкалы (без библиотек): шапка-недели + строки-треки + бар position:absolute. Фильтры: поиск/тип(objects)/активные/РП/статус/период(month/year/last12/custom)/масштаб(12-104 нед)/dateRange. RBAC: не-директор видит свои (responsible_pm_id / pm_id). Клик: тендер → #/tenders?open=, работа → WorkDetailModal из PmWorks. |
| `/gantt-works` | gantt.js + gantt_full.js | (тот же) | ✅ | Тот же компонент, kind=works |
| `/gantt-objects` | gantt.js + gantt_full.js | — | ✅ | Тот же компонент, kind=objects (объединённая шкала тендеров+работ) |
| `/readiness` | readiness.js | 245 | ✅ | РП-вид (и руководители). Hero (средняя готовность по проектам в подготовке + бейдж «горящих»). Сетка карточек: SVG-кольцо + название + статус + блокер + дни до старта. Клик → `StageDrawer` с детализацией по 7 этапам и кнопками override (`POST/DELETE /api/work-readiness/:workId/override`). PM видит только свои; HEAD_PM/директора/ADMIN — все в подготовке. Источник: GET `/api/work-readiness/summary?ids=…` для списка + `/api/work-readiness/:workId` для drawer. Реюз `Ring.jsx` и `StageDrawer.jsx` со страницей `/readiness-board`. |
| `/readiness-board` | readiness.js | (тот же) | ✅ | Сводка готовности по РП (директорский вид). Hero (общая средняя + всего горящих). Группировка `pm_id → works`, светофор 🔴/🟡/🟢 + средняя по РП + кольцо. Клик на РП → drawer со списком его проектов с кольцами, далее клик на проект → `StageDrawer` с этапами и override. RBAC: ADMIN/HEAD_PM/DIRECTOR_*. Поиск по ФИО РП. Реюз `Ring.jsx` и `StageDrawer.jsx`. |
| `/estimate-report` | estimate_report.js | 1 702 | ✅ | `pages/EstimateReport/index.jsx`. Отчёт по смете + AI. |
| `/approvals` | approvals.js | 641 | ✅ | Очередь согласования для директоров и HEAD_TO. Карточки просчётов с KPI (цена/себест/маржа), фильтры (поиск/режим sent vs all/РП), пагинация. EstimateApprovalModal: полный контекст (тендер, документы, cover_letter, лента approval_comments), 4 действия через ApprovalModal-паттерн с PromptModal для комментария. Endpoints: GET /api/estimates?status=sent, POST /api/approval/estimates/:id/{approve,rework,question,reject}, GET/POST /api/approval/estimates/:id/comments. |
| `/approval-payment` | approval_payment.js | 256 | ✅ | Очередь оплаты для бухгалтерии. Hero-карточка с балансом кассы. Поиск/фильтр. PaymentModal: 2 опции — 💳 ПП (комментарий → /pay-bank) и 💵 наличные (сумма+комментарий → /issue-cash с проверкой баланса). Кнопки «На доработку» и «Вопрос» через PromptModal. Лента комментариев. Endpoints: GET /api/approval/pending-buh, POST /api/approval/:entity/:id/{pay-bank,issue-cash,rework,question}. RBAC: только BUH/ADMIN. |
| `/bonus-approval` | bonus_approval.js | 576 | ✅ | Согласование премий рабочих. PM создаёт запрос (`BonusRequestModal`: работа → assignments → суммы по рабочим → обоснование, POST `/api/data/bonus_requests`). Директор/ADMIN решает через `BonusApprovalModal` (parseBonuses tolerant к JSONB/string/array, pill-распределение, 4 действия через `/api/approval/bonus_requests/:id/{approve,rework,question,reject}` с PromptModal). PM видит только свои (фильтр `created_by/pm_id == user.id`), статусы pending/approved/rejected/question/rework. Фильтр поиска + статуса. |
| `/kanban` | kanban.js | 470 | ✅ | Канбан-доска задач (4 колонки: 📥 Новые / 🔄 В работе / 👁 На проверке / ✅ Готово). HTML5 drag&drop (`PUT /api/tasks/:id/move`) с оптимистичным обновлением и откатом при 4xx. Фильтры: поиск (клиент), приоритет (low/normal/high/urgent), исполнитель (с пунктом «👤 Мои задачи»). Карточка: цветная левая полоса по приоритету, аватар (инициалы), дедлайн с тонами overdue/soon, счётчик комментариев, теги. `TaskDetailModal`: описание/исполнитель/дедлайн/файлы, лента комментариев (`GET/POST /api/tasks/:id/comments`), «Подтвердить ознакомление» (`PUT /:id/acknowledge`), «Подписаться/Отписаться» (`POST/DELETE /:id/watch`). Deep-link `#/kanban?id=N` открывает модалку, `asgard:tasks:changed` рефрешит. Без window.confirm/prompt/alert. |
| `/tasks-admin` | tasks_admin.js | 1 119 | ✅ | `pages/TasksAdmin/{index,api,TaskEditModal,TaskViewModal,tasks-admin.css}`. RBAC: ADMIN/DIRECTOR_*/HEAD_PM/HEAD_TO (см. `canAdminTasks`). KPI-ряд 4 (Новых/В работе/Выполнено/Просрочено) считается на клиенте через `buildKpi` от `/api/tasks/all`. 4 фильтра (поиск/статус/исполнитель/создатель) + матрица «РП × статус» с цветовой тонировкой ячеек (hot=err/warm=amber/cold=ok), клик по строке → фильтр. Таблица с подсветкой просроченных, sortBy id DESC сохранён. `TaskEditModal` (CRUD + multipart-файлы через `/files`). `TaskViewModal` (мета, описание, инструкции, файлы, действия — ✎ редактировать, ⚡ эскалировать (priority=urgent + `⚡ ЭСКАЛАЦИЯ:` в creator_comment через PromptModal), ✕ отменить (`PUT /:id/status` cancelled через PromptModal на причину), 🗑 удалить (ConfirmModal danger)). Endpoints: `/api/tasks/all`, `/api/tasks/stats`, `/api/tasks` POST, `/:id` PUT/DELETE, `/:id/status` PUT, `/:id/files` multipart, `/api/users?is_active=true&limit=500`. Audit: 75% (6/6 endpoints; vanilla-хелперы заменены на useModal+toast). |

## Группа 4 — Финансы (15 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/finances` | finances.js | 530 | ✅ | Деньги · Аналитика. Табы Расходы/Поступления + переключатель года (2020…tek) + сводка (за год / среднее / максимум). Гистограмма помесячно (clickable bars) → клик по столбику открывает детализацию месяца. SVG-кольцо по 11 категориям расходов (ФОТ/суточные/наличные/материалы/билеты/проживание/химия/оборуд/субподряд/трансфер/прочее) с легендой+%. Источники: `/api/expenses/work?date_from=Y-01-01&date_to=Y-12-31`, `/api/expenses/office?…`, `/api/works?limit=2000` (для advance/payment_date_fact). RBAC: ADMIN/BUH/DIRECTOR_*. Кнопка «📄 Импорт выписки» — редирект на legacy (модуль AsgardBankImport не мигрирован). |
| `/cash` | cash.js | 797 | ✅ | Казна Дружины (РП). `pages/Cash/{index,api,CreateRequestModal,DetailModal,ExpenseModal,ReturnModal,cash.css}`. Баланс-виджет 4 карточки + активные/завершённые секции + карточки заявок с прогресс-шагами (advance: 6 / loan: 5), deadline 12ч, баром бюджета. CreateRequestModal (Segmented advance/loan + SelectInput projects + MoneyInput). DetailModal с 5 действиями: Подтвердить / + Расход (ExpenseModal multipart с камерой) / Отчитаться / Погасить-Вернуть (ReturnModal) / Ответить (PromptModal). 7 категорий расходов. Deep-link ?id=NN. Без window.confirm/prompt/alert. Endpoints: GET /api/cash/my-balance, /my, /:id; POST /api/cash, /:id/expense (multipart), /:id/return, /:id/reply; PUT /:id/receive, /:id/submit-report; DELETE /:id/expense/:expenseId. RBAC: cash:read/write. |
| `/cash-admin` | cash_admin.js | 1 145 | ✅ | Казна. Управление (БУХ/директор). `pages/CashAdmin/{index,api,BalanceAdjustModal,CloseRequestModal,DetailModal,cash-admin.css}`. Баланс кассы (gold 32px) + BalanceAdjustModal (POST /balance/adjust) + лента 10 операций. 5 KPI (Ожидают решения/выдачи/Просроченные/Активных/На руках у РП). 3 секционных баннера (ТРЕБУЮТ РЕШЕНИЯ/ОЖИДАЮТ ВЫДАЧИ/ПРОСРОЧЕНЫ ПОДТВЕРЖДЕНИЯ/ОЖИДАЮТ ВОЗВРАТОВ). Табы Заявки/Сводка. DetailModal с 5 действиями: Согласовать (ConfirmModal success) / Выдать (ConfirmModal gold) / Отклонить (PromptModal) / Вопрос (PromptModal) / Закрыть (CloseRequestModal с force при остатке). Сводка: таблица сотрудник/выдано/потрачено/возвращено/на руках + ИТОГО. Endpoints: GET /api/cash/all, /summary, /balance, /:id; PUT /:id/approve|issue|reject|question|close, /:id/return/:returnId/confirm; POST /balance/adjust. RBAC: cash_admin:read/write (BUH/ADMIN/DIRECTOR_*). |
| `/buh-registry` | buh_registry.js | 560 | ✅ | Реестр расходов (BUH). 6 фильтров: год (по умолч. текущий), месяц, заказчик (из works), кто внёс (только реально вносившие), категория (12), статус СФ (Нужна/Получена/Не нужна). Сводка 3-6 карточек: всего/ожидают СФ/СФ получены + топ-3 категории по сумме. Таблица: дата, категория (badge с иконкой+цветом), сумма, заказчик/работа (с ФОТ-детализацией), статус работы, поставщик, № док., СФ-pill, кто внёс. ExpenseViewModal (read-only с ФОТ-секцией). ExpenseEditModal (только doc_number + invoice_needed + invoice_received, как в vanilla). CSV-экспорт. Пагинация 25/стр. Endpoints `/api/expenses/work?limit=10000` + `PUT /api/expenses/work/:id`. RBAC: ADMIN/BUH/DIRECTOR_*. |
| `/invoices` | invoices.js | 332 | ✅ | Счета и оплаты. Список со статусами (Черновик/Выставлен/Ожидает/Частично/Оплачен/Отменён, цветной pill), поиск (номер/контрагент/ИНН/описание), фильтр статуса, пагинация 25, сводка (Σ сумма / Σ оплачено) в subtitle. Клик по строке → InvoiceDetailModal (карточка + лента платежей + кнопки 📄 PDF / 💰 Внести оплату / ✎ / 🗑). InvoiceEditModal (CRUD с авто-номером СЧ-ГГГГ-NNN через GET /next-number, выбор контрагента из /api/customers, привязка к работе, сумма+НДС%+авто-итого, due_date, описание). PaymentModal (POST /:id/payments — бэк сам пересчитывает paid_amount+status). Deep-link `#/invoices?id=NN`. PDF в новой вкладке через `?token=` query. RBAC на запись: ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/PM/BUH. Endpoints: `/api/invoices` (GET/POST/PUT/DELETE), `/:id/payments`, `/:id/pdf`, `/next-number`. |
| `/acts` | acts.js | 240 | ✅ | Акты выполненных работ. Список с 4 статусами (Черновик/Отправлен/Подписан/Оплачен), поиск, фильтр, пагинация 25. Сводка: count + Σ суммы + Σ подписанных. ActDetailModal (карточка + действия: 📄 PDF / ✍ Подписан (ConfirmModal success → signed + signed_date) / 💰 Оплачен (ConfirmModal gold → paid + paid_date) / ✎ / 🗑). ActEditModal (CRUD с авто-номером АКТ-ГГГГ-NNN через /next-number, контрагент из /api/customers, привязка к работе, amount+vat_pct+авто-total, signed_date, description). Deep-link `?id=NN`. RBAC на запись: ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/PM/BUH. Endpoints: `/api/acts` (GET/POST/PUT/DELETE), `/:id/pdf`, `/next-number`. |
| `/payroll` | payroll.js | 1 584 | ✅ | Расчёты с рабочими. Единый корень `Payroll/` с переключателем видов по URL (`?view=grid`, `?id=N`, mode-prop). **PayrollList** — табы статусов (Все/Черновик/На согл./Согл-но/Оплачено/Доработка), KPI 4 (ведомостей / ожидают согл. / к выплате / выплачено), поиск + фильтр по работе (PM видит только свои), пагинация 25, карточки с цветным StatusBadge + период/рабочие/начислено/к выплате. Создание ведомости через `SheetCreateModal` (Работа + период с/по через DatePicker + название + комментарий, авто-период = пред.месяц). **PayrollSheet** (`/payroll-sheet?id=N`) — карточка ведомости с inline-редактированием полей (days_worked/day_rate/bonus/penalty/advance_paid, дебаунс 800мс через `updateItem`), KPI 7 (начислено/премии/удержания/авансы/к выплате/рабочих/ср.ставка), кнопки действий по статусу+роли: `+ Добавить` → **PaymentModal** (Combobox по сотрудникам + автоподтяжка ставки через `currentRate` + дни/премия/штраф/аванс/удержание/способ выплаты/комментарий + локальный пересчёт), `⚡ Автозаполнение` (`autoFillItems` для work_id), `↻ Пересчитать` (`recalcItems`), `📤 На согласование` (ConfirmModal), `✓ Согласовать`/`↻ На доработку` (PromptModal с причиной, для директоров), `💰 Оплачено` → **SheetCloseModal** (показывает итоги + `paySheet` создаёт `payment_registry` + ФОТ-расходы), `📥 Excel` → **ExportModal**, `🗑 Удалить` (ConfirmModal danger), реестр выплат + комментарии + директорский комментарий при rework. **PayrollGrid** (`/payroll-grid` или `?view=grid`) — Excel-сетка табеля: селекторы Месяц/Год + `Сформировать` (`loadPayrollGrid`), KPI 4 (рабочих/смен/ФОТ/суточные), таблица фио × 1..31 дни + Дней/Баллов/Заработок/Суточные/ИТОГО + tfoot, цветовые ячейки через `pointsCellStyle`+`buildPointsMap` из `tariff_categories`, режим `✏ Редактировать` (inline-инпуты → pendingEdits → `💾 Сохранить` через `savePayrollGrid`), `📥 Excel` через `exportPayrollGrid`. **PayrollReport** (`/reports/payroll`) — годовая статистика (`loadStats`): KPI 6 (начислено/выплачено/ожидают/ведомостей/рабочих/ср.ставка), бары по месяцам (accrued + paid-сегмент), топ работ по ФОТ (10), топ рабочих по выплатам (10) + фильтр-таблица `payment_registry` (год/статус/тип/период через `loadPayments`, ограничение 500 строк). **ExportModal** — выбор режима payments/grid + период для grid (`MONTHS_RU` + год) + скачивание blob через `exportPaymentsExcel`/`exportPayrollGrid`. RBAC: VIEW=ADMIN/DIRECTOR_*/PM/HEAD_PM/BUH; APPROVE=ADMIN/DIRECTOR_*; PAY=ADMIN/DIRECTOR_*/BUH; CREATE=ADMIN/DIRECTOR_*/PM/HEAD_PM. Endpoints: GET/POST/PUT/DELETE `/api/payroll/sheets`, `/items`, `/items/auto-fill`, `/items/recalc`, `/sheets/:id/{submit,approve,rework,pay}`, GET `/api/payroll/rates`+`/current`, GET `/api/payroll/payments`+`/export`, GET `/api/payroll/stats`, GET/PUT `/api/worker-payments/reports/payroll-grid/:y/:m{,/save,/export}`. Без `window.confirm/prompt/alert` — только `ConfirmModal`/`PromptModal`/`toast.*`. |
| `/payroll-dashboard` | payroll_dashboard.js | 445 | ✅ | index/api/AgreementTransferModal/FinanceLimitsModal. Доступ ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/BUH + любой DIRECTOR_*. Период ◀/Месяц/Год/▶ + KPI 4 (заработано/перевод/из кассы/возврат). Quick-навигация к 4 ведомостям (Самозанятые/Официально/PmBalance/OneTimePay). 1) Расчёт кассы: GET /cash-calc/:y/:m с табами Все/Самозанятые/Официальные/Наличка + tfoot ИТОГО + итог-в-кассе. 2) Операции СЗ: GET /se-transfers/:y/:m + фильтр work/agreement + кнопка «✅ Наличные получены» (PUT /confirm-return) + «✕ Отмена» (PUT /cancel) + summary (переводов/сумм). Создание agreement_transfer через AgreementTransferModal (Combobox по СЗ с остатком лимита + MoneyInput + комментарий → POST /se-transfers). 3) Годовые лимиты: GET /self-employed-limits с прогресс-баром +подсветкой <2 месячных, для ADMIN/DIRECTOR_GEN — FinanceLimitsModal (GET/PUT /api/admin/system/settings/finance-limits). |
| `/payroll-sheet` | (вкл. в payroll) | — | ✅ | См. `/payroll` — карточка одной ведомости через `Payroll mode="sheet"`. Hash-параметр `?id=NN`. |
| `/payroll-grid` | (вкл. в payroll) | — | ✅ | См. `/payroll` — Excel-сетка через `Payroll mode="grid"` (или `/payroll?view=grid`). |
| `/office-expenses` | office_expenses.js | 603 | ✅ | index/api/OfficeExpenseFormModal/OfficeExpenseDetailModal. RBAC: VIEW=ADMIN/OFFICE_MANAGER/BUH/DIRECTOR_*, CRUD=ADMIN/OFFICE_MANAGER + автор, APPROVE=DIRECTOR_*. 12 категорий (rent/utilities/office_supplies/communication/transport/household/office_equipment/software/representation/project_expense/tools/other). KPI 4 (всего за год/среднее/на согласовании/согласовано) + сетка категорий с суммами (кликабельная — фильтр). Поиск + Год/Месяц + табы по статусу (Все/На согласовании/Согласовано/Отклонено/Черновики). Таблица расходов с группировкой по статусу. FormModal: дата/категория/сумма/поставщик/№док/комментарий + СФ нужна/получена + кнопки «Сохранить черновик» / «Сохранить и отправить». DetailModal: статус + KPI + лента approval_comments + действия по роли (📤 send, ✓ approve, ✕ reject с PromptModal, 🔄 rework, ✎ edit, 🗑 delete). Endpoints: GET/POST `/api/expenses/office`, PUT/DELETE `/api/expenses/office/:id`, POST `/api/approval/office_expenses/:id/{send,approve,reject,rework}`, GET `/api/approval/office_expenses/:id/comments`. |
| `/one-time-pay` | (вкл. в payroll, renderOneTimePay) | ~250 | ✅ | Разовые оплаты (такси/топливо/питание). 5 табов (Все/Ожидают/Согласовано/Оплачено/Отказ), карточки с типом+бейджем. `OneTimeCreateModal`: Combobox по employees + сумма + тип (6 типов с иконками) + работа (PM видит только свои) + причина. Действия (RBAC): директор → Согласовать/Отклонить (через ConfirmModal/PromptModal), BUH/директор → Оплачено. Endpoints `/api/payroll/one-time` (GET с фильтром по статусу, POST, PUT `/:id/{approve,reject,pay}`). |
| `/self-employed` | (вкл. в payroll, renderSelfEmployed) | ~300 | ✅ | Реестр самозанятых (НПД). Поиск + фильтр статуса НПД (active/suspended/closed) + сетка карточек (ФИО, ИНН, телефон, банк, ****последние 4 счёта, ГПХ). `SelfEmployedEditModal` (CRUD: ИНН 12-цифр валидация на клиенте и бэке, контролы INNInput/PhoneInput, дата ГПХ, статус НПД, комментарий). `SelfEmployedDetailModal` (реквизиты + история выплат `/self-employed/:id/payments` + KV-таблица + итог выплачено). Доступ: ADMIN/BUH/DIRECTOR_*. Endpoints `/api/payroll/self-employed`. |
| `/pm-balance` | pm_balance.js | 478 | ✅ | Баланс РП (наличные на руках). KPI-полоса (получено/потрачено/на руках/в минусе). Таблица «РП × cash_in/se_cash_in/потратил/вернул/на руках» с цветом по балансу (зелёный/красный/нейтральный). Клик по строке → `PmBalanceDetailModal` с 5 секциями операций: 💵 Получено из кассы, 🔄 От СЗ, 💳 Выплаты наличкой, 🧾 Расходы, ↩️ Возвраты в кассу (каждая с итогом). Фильтр по месяцу (клиентский). Доступ: ADMIN, DIRECTOR_GEN, DIRECTOR_COMM, BUH. Endpoints `/api/payroll-dashboard/pm-balance` + `/:pm_id` с нормализацией полей (cash_out=expenses+salaries). |
| `/reports/payroll` | (новый, объединён с payroll) | — | ✅ | См. `/payroll` — отчёт по выплатам через `Payroll mode="report"`. Годовая статистика `/api/payroll/stats` (KPI + бары по месяцам + топ работ/рабочих) + фильтр-таблица `payment_registry` (статус/тип/период) + экспорт Excel. |

## Группа 5 — Ресурсы / Закупки / Склад (10 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/procurement` | procurement-page.js | 1 031 | ✅ | Реестр заявок на закупку. Канбан 6 объединённых колонок (🆕 sent_to_proc / 🛠️ proc_responded / ⏳ pm_approved+dir_question+dir_rework / 💳 dir_approved+paid / 🚚 partially_delivered+delivered / ✅ closed+dir_rejected) с HTML5 drag&drop и картой разрешённых переходов по ролям (`kanbanActionFor`). Таблица с сортировкой+пагинацией. Дашборд KPI (pending/overdue/upcoming/paid — для PROC/ADMIN/DIR). Переключатель Канбан/Таблица (LS proc_view_v2). Поиск+статус-фильтр. `ProcurementDetailModal` (модалка деталей): inline-edit позиций (наименование/кол-во/поставщик/цена для PROC), подсказки цен из истории (батч `/price-hints` last+market stats + кнопка «подставить» для PROC), сплит позиций по поставщикам (`SplitItemModal`), откат сплита, загрузка счёта (`InvoiceImportModal`: Excel multipart / PDF pdf.js / фото Tesseract.js → /invoice/parse → fuzzy-матчинг → /invoice/:importId/apply), витрина каталога (`ShowcaseModal`: /api/products/catalog-procurement → корзина с расчётом «докупить = need − available» → POST /items/bulk), + Позиция вручную с подсказкой цены при вводе, добавление списком (POST /items/import-text), AI-разбор ТЗ (POST /items/ai-parse), импорт Excel (`ImportExcelModal`), прикрепление файла-счёта к позиции (POST /api/files → PUT item.invoice_doc_id), удаление, группировка по категории/поставщику (UI), приёмка (`DeliverModal`: выбор позиций+ячейка → PUT /:itemId/deliver, золотое подсвечивание eq-created), счета поставщиков (видны бухгалтеру), платёжки, история (timeline). Цепочка согласования: send-to-proc → proc-respond → pm-approve → dir-approve(locked)/rework/question/reject → mark-paid → deliver-items → close. Кнопки видны по статусу+роли (`getActions`). Комментарий обязателен для rework/question/reject/return. Шаблоны заявок (`/templates` + создание из шаблона + сохранить как шаблон). Клонирование заявки. `CreateProcurementModal` (autoShowcase=true по умолчанию). Экспорт реестра/заявки в Excel с группировкой по поставщикам. Coverage audit: **97%** (27/27 endpoints, 6/7 модалок — последняя `openModal` это base `useModal().open`). |
| `/my-procurement` | (тот же) | — | ✅ | Тот же компонент `Procurement` с пропом `mode="my"`. Фильтр `pm_id=user.id` на server-side. Заголовок «Мои заявки на закупку». Без дашборда. `?work=N` фильтр по работе (для перехода из карточки работы через `gotoLegacy('my-procurement', { work: w.id })`). |
| `/warehouse` | warehouse.js | 1 346 | ✅ | Редирект `<Navigate to="/warehouse-v2" replace />` в `App.jsx` (старая страница ушла, vanilla AsgardWarehouse заменена на `WarehouseV2`). |
| `/warehouse-v2` | warehouse-v2.js + warehouse-v2-equipment.js | 1 256 + 701 | ✅ | `pages/Warehouse/` (20 файлов, ~3800 строк). **5 вкладок:** 🛠️ Оборудование · 🧰 Расходники · 🚚 Приёмка · 🗺️ Ячейки · 📜 Движения. **KPI-шапка** (каталог/строк наличия/ниже минимума/ячеек) + **поиск с автоподсказками** на Расходники и Оборудование. **Степперы «+ В корзину» ⇄ [−][N][+]** (Ozon-стиль) на расходниках/оборудовании; equip=поштучно. **FAB-корзина** (угол справа-снизу) с бейджем-счётчиком + мини-разбивкой (резерв/закупка). **`CartDrawer`** (выезжает справа, blur-фон): список с степперами, привязка к работе на каждой позиции, ↻ обновить остатки, ＋ Добавить вручную (поиск каталога + новая позиция), 📎 Excel (POST `/parse-excel`), 👁 Предпросмотр (POST `/preview-submit`), Отправить (POST `/submit` с обработкой 409 stock_changed/equipment_taken — подсветка изменившихся). **Складские операции** (`StockOpModal`): 📥 Приход / 📤 Расход / 🔄 Перемещение / 🗑️ Списание через `/api/stock/{receipt,issue,transfer,writeoff}`. **`QuickProductModal`** — быстрое создание позиции каталога. **`CatalogImportModal`** (УПД/счёт Excel) — POST `/api/catalog-import/excel` + редактируемая таблица + POST `/:id/apply` (PDF/фото OCR через pdf.js+Tesseract — отложено). **`ProductDetailModal`** — фото, остатки по ячейкам, последние движения, последняя цена, подтверждение черновика. **Оборудование:** KPI (всего/на складе/выдано/в ремонте/балансовая ст.) с кликом=фильтр, статус-чипы, фильтры категорий/складов, карточки/таблица (toggle), `EquipmentFormModal`, `EquipmentIssueModal` (РП+объект+работа+состояние), `EquipmentReturnModal`, `EquipmentTransferModal`, `EquipmentCardModal` (4 таба: Инфо/Перемещения/ТО/QR). Запросы на передачу для ADMIN/WAREHOUSE с кнопками ✅/❌ (transfer-execute / requests/:id/reject). **Заявка на выдачу** (PM): `EquipmentRequestCartModal` — корзина из `/available-for-request` + работа/сроки → POST `/requests/batch`. **Экран кладовщика** (`EquipmentBatchesModal`): подтвердить (PUT `/batch/:id/approve`), отклонить (PUT `/batch/:id/reject`), убрать позицию (DELETE `/requests/item/:id`). Поиск по QR/инв.№ (PromptModal → `/by-qr/:code`). Excel-экспорт оборудования через `?token=`. **Ячейки:** сетка карточек + `BulkLocationsModal` (зона/стеллажи/полки/ячейки → POST `/locations/bulk`). **Движения:** журнал с типами/маршрутами/авторами. **Приёмка:** 2 секции (на склад / напрямую на объект) с overdue-подсветкой. Все цвета через CSS-vars, mobile-адаптивно (drawer=100vw, FAB меньше). Endpoints: `/api/equipment/*` (25+), `/api/stock/*` (10+), `/api/warehouse-cart/*` (7), `/api/warehouse/locations/*` (2), `/api/catalog-import/{excel,:id/apply}`, `/api/products` + `/api/product-categories` + `/api/works`. Audit: 75% TasksAdmin · 68% Warehouse (реальное ~95% — vanilla `openModal/openCartDrawer/openSubmitPreview/openProduct/openBulk` это наши `useModal+CartDrawer/PreviewBlock/ProductDetailModal/BulkLocationsModal`; единственное реально отложенное — `/api/catalog-import/ai` для PDF/photo OCR). |
| `/my-equipment` | my_equipment.js | 276 | ✅ | index/api/ReturnModal/TransferModal. Доступ: любой залогиненный (бэк отдаёт по userId). Загружает GET `/api/equipment/by-holder/:userId`. KPI 4 (всего/на объектах/к работам/сломано). Поиск по name/inv.№/S/N/объекту. Таблица с группировкой по категориям + multi-select чекбоксы (включая «выбрать все»). 2 действия по выбранным: «📥 Вернуть на склад» (ReturnModal: condition+notes → POST `/api/equipment/return` циклом, success/fail отдельно) и «📤 Запрос на передачу» (TransferModal: Combobox по РП через `/api/users?role=PM` минус себя + SelectInput работ через `/api/works` отфильтровать активные + Объект через `/api/equipment/objects` + notes → POST `/api/equipment/transfer-request` циклом). Состояние оборудования через StatusBadge (new/good/satisfactory/poor/broken). Без модалок-заглушек, реальный CRUD. |
| `/suppliers-catalog` | suppliers-page.js | 673 | ✅ | `pages/SuppliersCatalog/{index,api,SupplierEditModal,SupplierDetailModal,PriceRecordModal,suppliers-catalog.css}`. 2 таба: «Поставщики» (фильтры поиск/категория/is_active, таблица с рейтингом-звёздами+контактами+статусом) и «База цен» (фильтры поиск/source/период). `SupplierEditModal` (CRUD: имя+ИНН+КПП+ОГРН+тел+email+сайт+адрес+категория из 4+рейтинг+notes+is_active Switch). `SupplierDetailModal` (реквизиты+pills+статистика deals/price_points/last_price+top-товары+контакты CRUD с primary-star+история цен последние 50; кнопки ✎/🗑 ADMIN). Inline `ContactEditModal` (ФИО+должность+тел+email+telegram+is_primary Switch). `PriceRecordModal` (item_name+unit_price MoneyInput+unit+supplier Combobox+source SelectInput+source_url+notes). Endpoints: GET/POST/PUT/DELETE `/api/suppliers`, `/api/suppliers/:id/stats`, `/api/suppliers/:id/price-history`, `/api/suppliers/:id/contacts` CRUD, `/api/price-records`. RBAC: READ=PROC/ADMIN/PM/HEAD_PM/DIR_*/BUH/WAREHOUSE, WRITE=PROC/ADMIN, DELETE=ADMIN. Event `asgard:suppliers:changed`. **Coverage 100%**. |
| `/assembly` | assembly-page.js + assembly-dnd.js | 211+605 | ✅ | `pages/Assembly/{index,api,AssemblyCreateModal,AssemblyDetailModal,AddItemModal,assembly.css}`. Карточки ведомостей (mob/demob/transfer) с прогресс-баром. Фильтры тип+статус (8)+поиск. `AssemblyCreateModal`: work Combobox из `/api/works`+type (mob/transfer; demob — отдельной кнопкой)+title+destination+planned_date+notes. `AssemblyDetailModal`: KPI (позиций/собрано/прогресс/паллет), метаданные, паллеты grid с capacity (over-limit подсветка), позиции таблицей с Select для назначения на паллет+pack-toggle+для демоба SelectInput return_status (returning/damaged/lost→PromptModal с причиной /consumed)+источник (со склада/закупка→объект/закупка→склад/вручную/купл. на объекте). `AddItemModal`: автокомплит по `/api/products/search`+source. Действия по статусу+роли: ✅ Подтвердить (draft→confirmed, PM/HEAD_PM/ADMIN/DIR), 🚛 Отправить (→in_transit, PM/WAREHOUSE), 🏠 Демоб (mob→создать demob), 📦 Принять (demob→returned, WAREHOUSE возвращает stock/equipment+списания); 🖨️ Чек-лист + 📥 Excel через `?token=`. `PalletCreateModal` (label+capacity_items+capacity_kg+notes). Hash deep-link `?id=`. Event `asgard:assembly:changed`. Без DnD-эффектов vanilla (Web Audio thud/FLIP/ripple) — это эффекты UX, не функция; основа сбора реализована полностью. **Coverage ~95%**. |
| `/pass-requests` | pass-requests-page.js + backend pass_requests.js | ~ | ✅ | См. Группу 9 (Документы) |
| `/tmc-requests` | tmc-requests-page.js | 192 | ✅ | `pages/TmcRequests/{index,api,TmcRequestModal}`. Таблица заявок (название/проект/сумма/приоритет/статус 7 опций/дата). KPI: всего/сумма/ожидают/доставлено. Фильтры поиск/статус/приоритет. `TmcRequestModal`: title required + work Combobox + priority (low/normal/high/urgent) + needed_by DatePicker + supplier + delivery_address + динамический список позиций (name/unit/qty/price → auto total) + notes + auto total_sum. Действия со строкой по роли: Excel каждой через `?token=`, директор/BUH approve/reject (PromptModal причина) / mark ordered / mark delivered / закрыть. Hash deep-link `?id=`. Backend проверяет редактируемость (draft/submitted). Endpoints: `/api/tmc-requests` CRUD, `/:id/status`, `/export?token=`, `/:id/excel?token=`. RBAC WRITE: ADMIN/PM/HEAD_PM/TO/HEAD_TO/DIR_GEN/DIR_COMM/BUH; APPROVE: ADMIN/DIR_GEN/DIR_COMM/BUH. Event `asgard:tmc-requests:changed`. **Coverage 100%**. |
| `/equipment` | (вкл. warehouse) | — | ✅ | Алиас: `<Navigate to="/warehouse-v2" replace />`. |

## Группа 6 — Кадры / Дружина (15 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/personnel` | personnel.js | 763 | ✅ | Реестр «Дружина»: 5 кликабельных бейджей по статусу (on_site/approved/ready/not_ready/archive), фильтры (поиск+специальность+статус), таблица с группировкой по статусу + документами 🔴/⚠️/✅, СЗ-прогресс-бар 2.4М ₽, рейтинг. AddEmployeeModal (POST `/api/staff/employees`), EmployeeDetailModal (deep-link `#/employee?id=`) с KPI/сменой статуса (PUT `/readiness/:id/status` с датой/причиной)/СЗ-логом/PII только HR-ADMIN-директорам/последними 10 оценками, EditEmployeeModal (PUT `/api/staff/employees/:id`) с 5 секциями. ReviewModal (POST `/:id/review`). Источник: GET `/api/staff/readiness`. RBAC: VIEW=HR/PM/HEAD/директора/TO/HEAD_TO/OFFICE_MANAGER, EDIT=HR/ADMIN/директора (PM read-only), PII=EDIT_ROLES. |
| `/employee` | (вкл. в personnel) | — | ✅ | Открывается как модалка из /personnel по deep-link `#/employee?id=`. Полная анкета (legacy 755 строк) — кнопка «📋 Анкета» делает редирект на legacy. |
| `/hr-requests` | hr_requests.js | 911 | ✅ | Заявки персонала v2: PM видит `/api/staff-requests/my`, HR — `/pending`. Фильтр по статусу + поиск + опц. фильтр work_id. Hash-парам: `?create=1[&work_id=]` открывает форму, `?id=` — карточку. **RequestFormModal** (PM создаёт/редактирует): объект из `/api/works?my=1`, 8 позиций (master/fitter/welder/pto/chemist/insulator/assembler/laborer), даты, условия (питание/жильё/ротация). Блок «Требуемые допуска по должностям» — для активных ролей + «для всех», типы из `/api/permits/types`, кастомные допуска с категорией, маркер «не требуются», 409-разбор missing_roles при submit с подсветкой ролей. **PmViewModal** (PM смотрит): прогресс позиций, назначенные рабочие, «✎ Редактировать» (draft/rework) и «+ Добавить в бригаду» (approved через ConfirmModal). **HrSplitModal** (HR подбор, split-screen): авто PUT `/:id/take` при new, слева детали+позиции+назначенные с «✕ Убрать», справа доступные из `/:id/available-workers` (поиск+фильтр по роли, conflict_reason role_mismatch ⚠️ / missing_permits 🔴, ★ рейтинг), кнопки sent_to_pm / approve (ConfirmModal) / rework (PromptModal). |
| `/hr-rating` | hr_rating.js | 196 | ✅ | Рейтинг рабочих (офисные роли отсеиваются по OFFICE_ROLES). Источник GET `/api/staff/readiness` (vanilla читала локальную IndexedDB — в v2 берём rating_avg/rating_count с сервера). Топ-3 🥇🥈🥉. Таблица: место/ФИО/роль/город/рейтинг (цветная точка по 8/6/<4)/оценок/последняя. Поиск ФИО/роль/город, пагинация 50. Клик по строке → EmployeeDetailModal (реюз из /personnel). RBAC: ADMIN/HR/HR_MANAGER/директора. |
| `/collections` | employee_collections.js | — | ✅ | `pages/Collections/index.jsx`. Подборки дружины. |
| `/permits` | permits.js | 1 314 | ✅ | Разрешения и допуски (M6). `pages/Permits/{index,api,ListTab,MatrixTab,ProjectsTab,PermitEditModal,RenewModal,BulkRenewModal,ManageTypesModal,permits.css}`. 5 KPI (Всего/Действующих/Истекают 30д/14д/Истекли с цветной обводкой). Табы Список/Матрица/Проекты. **ListTab**: фильтры (status: expired/expiring_14/30/active + 11 категорий + Combobox сотрудник + ↻ Обновить + + Добавить). Чекбоксы + bulk-выбор + Bulk Renew (даты + apply). Таблица: сотрудник, цветная mark по категории + название типа, номер, выдано/до, status-pill, действия (📎 Скан / Продл / Изм / Уд). PermitEditModal (Combobox сотрудник в режиме создания, SelectInput тип с 11 категориями-группами, dок номер+issuer, даты, multipart файл-скан). RenewModal (старый деактивируется, новый с suggested expiry из validity_months). BulkRenewModal (POST /bulk-renew с массивом). **MatrixTab**: матрица сотрудники × типы (sticky-колонка слева, диагональные заголовки), required-фильтр по проекту с *. **ProjectsTab**: Combobox проектов → 2 секции (требования сгруппированные по должностям + готовность команды). Добавление требования (Должность+Тип+Обязательный) или маркер «не требуются». Готовность команды показывает per-employee бейджи допусков + ИТОГО OK/Не хватает. ManageTypesModal (CRUD типов через permit-applications/types). RBAC: ALLOWED_READ_ROLES, WRITE_ROLES (ADMIN/HR/TO/HEAD_TO/HR_MANAGER), TYPE_MGMT_ROLES. Авто-проверка уведомлений раз в день при заходе + кнопка «🔔 Проверить уведомления». Без window.confirm/prompt/alert. Endpoints: GET /api/permits, /types, /stats, /:id, /matrix, /work/:wId/requirements, /work/:wId/compliance, /upcoming, /check-expiry; POST /api/permits (multipart), /:id/scan, /:id/renew, /bulk-renew, /work/:wId/requirements, /api/permit-applications/types; PUT /:id; DELETE /:id, /work/:wId/requirements/:id, /api/permit-applications/types/:id. **Полировка 14.06.2026 → 9.5+/10:** Hero «Готовность дружины к работам» с прогресс-баром (зелёный ≥80%, амбер ≥60%, красный <60%) + meta-блок с истёкшими/истекающими 14-дн. Кастомные `.pmt-tab` заменены на стандартный `TabsBar` с иконками 📋 Список / 📊 Матрица / 🏗 Проекты. На карточках KPI добавлены иконки ✅/⏰/❌ и pulse-анимация для горящих секций. В матрице sticky-заголовки сверху (top:0 + z-index:3) + max-height 100vh-320px для скролла. |
| `/permit-applications` | permit_applications.js | 1 278 | ✅ | Заявки на оформление разрешений (M6). `pages/PermitApplications/{index,api,FormPage,EmployeeSelectModal,PermitSelectModal,SendConfirmModal,ViewModal,permit-applications.css}`. **index.jsx** (список): 4 KPI (Черновики/Отправлены/В работе/Завершены), 5 статус-табов (Все/Черновики/Отправлены/В работе/Завершены с счётчиками), поиск, таблица (номер/подрядчик+title/Сотр./Разр./статус-pill цветной/дата/действия по статусу). Действия: ✎ редактировать (только draft), 👁 просмотр (ViewModal), 📥 Excel (downloadExcel→blob), ✉ Отправить (SendConfirmModal с copy_to_self), ⚙/✓ статус-переходы (ConfirmModal), 🗑 удалить (ConfirmModal danger). Deep-link ?id=NN. **FormPage** ( или ): подрядчик через Combobox с автокомплитом (/contractors → onSelect автозаполняет email), таблица сотрудников с per-row кнопкой убрать + бейджи разрешений по категориям + кнопка «+ ещё» (PermitSelectModal) + Notes. Sticky footer: Сохранить черновик / Скачать Excel / Отправить подрядчику. Сопроводительное письмо в свёртке. **EmployeeSelectModal**: чекбоксы сотрудников, поиск + role-фильтр (уник role_tags), Выбрать всех/Снять, счётчик. **PermitSelectModal**: 11 категорий-карточек (с иконками+counter selected), drill-down в категорию → плоский список с иконками+статусами (active/expiring/expired) для каждого типа + 5 пресетов (basic/offshore/gas/electric/welding) + «📋 Скопировать с» с другого сотрудника + поиск (плоский). **SendConfirmModal**: кому/тема/вложение + copy_to_self checkbox. **ViewModal**: read-only с подрядчиком+статусом+сотрудниками+историей переходов статуса. Без window.confirm/prompt/alert. Endpoints: GET /api/permit-applications, /:id, /types, /contractors, /:id/excel (blob); POST /api/permit-applications, /:id/status, /:id/send; PUT /:id; DELETE /:id. RBAC: ALLOWED_ROLES (8 ролей) + CREATE_ROLES (5). |
| `/permit-application-form` | (тот же) | — | ✅ | См. `/permit-applications` — FormPage в . Хеш-параметр  редактирует существующий черновик. |
| `/training` | training-applications-page.js | — | ✅ | `pages/Training/index.jsx`. Заявки на обучение. |
| `/training-board` | training_board.js | 444 | ✅ | index/api/DetailModal/CompleteModal. RBAC: ADMIN/TO/HEAD_TO/DIRECTOR_GEN. GET /api/training/pending → таблица: рабочий+должность, объект, не хватает допуска+тип, дедлайн (overdue/<7д бейджи), статус, ссылка на сертификат (?token=), действия. ▶ Начать (PUT /:id/start), ✅ Завершить (multipart upload файла + PUT /:id/complete с certificate_number/valid_from/valid_to/trainer_name — создаёт запись в employee_permits если есть valid_to), 👁 Детали. Сводка: ожидает/в процессе/просрочено/<7д. Поиск + фильтр статусов. |
| `/office-schedule` | office_schedule.js | 364 | ✅ | index/api/StatusPickerModal/css. Календарная сетка месяц × сотрудники с 10 статусами (оф/уд/об/бн/сс/км/пг/уч/ск/вх). Sticky колонка ФИО и шапка дней, выходные выделены, сегодня — золотой бордер. Клик по ячейке → StatusPickerModal с SelectInput. Сид staff из активных users (без BOT/ADMIN/FIELD_WORKER/test_/mimir_bot). Все статусы через `/api/data/staff_plan` (POST/PUT/DELETE), staff seed через `/api/data/staff`. RBAC: сотрудник редактирует только свою строку; OFFICE_MANAGER — любую; ADMIN/директора — если в settings.app `schedules.office_strict_own=false`. |
| `/workers-schedule` | workers_schedule.js | — | ✅ | `pages/WorkersSchedule/{index,api,StatusPickerModal}`. График рабочих по объектам + статусы смен через `/api/data/staff_plan`. |
| `/travel` | travel.js | 586 | ✅ | index/api/TravelAddModal/TravelUploadModal/css. KPI 4 (всего/сумма/отправлено/ожидают), 4 таба (Билеты/Жильё/Направления МО/Обучение), SearchInput, грид карточек. Создание через `/api/field/logistics` POST (тип/сотрудник/маршрут/datetime-local вылет-прилёт/№ рейса/датыС-По/сумма+НДС/проект/файл) — для билетов авто-создание этапа `field_trip_stages` и расход в `work_expenses`. Действия со строкой: 📎 Файл (TravelUploadModal с multipart), ✅ Куплено (POST `/:id/purchased`), 📨 Отправить (POST `/:id/send` → SMS+Push сотруднику), 🗑 (ConfirmModal → DELETE через `/api/data/field_logistics/:id`). RBAC: ADMIN/OFFICE_MANAGER/HR/PM/HEAD_PM/директора/TO. |
| `/official-employees` | official_employees.js | 384 | ✅ | index/api/OfficialEmployeeEditModal/css. Таблица оклада + несгораемой + статуса + долга компании, KPI 4 пилюли (всего/работают/в отпуске/долг). Просмотр — ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/BUH; редактирование оплаты — ADMIN/DIRECTOR_GEN/BUH; редактирование ПД (паспорт/ИНН/СНИЛС/адрес) — ADMIN/HR/HR_MANAGER/DIRECTOR_GEN (вкладка «🆔 Паспорт» скрыта от остальных). Модалка с табами 💰 Оплата (оклад/несгораемая/статус + период leave для отпуска/декрета/больничного + переключение тип занятости в самозанятые/обратно через ConfirmModal) и 🆔 Паспорт (серия/№/выдан/дата/код подразделения/ДР/место рождения/адрес рег/ИНН/СНИЛС с предупреждением о ПД). Backend: PUT `/api/payroll-dashboard/official-employees/:id` (оплата) + PUT `/api/staff/employees/:id` (ПД, lazy-load при открытии вкладки). |
| `/global-timesheet` | global_timesheet.js | 287 | ✅ | index/api/TypePickerModal/css. Sticky-таблица месяц × рабочие с группами по работам. 6 типов ячеек (Д/Н/🚗/📦/🏥/⏳) с цветами из CELL_TYPES. Навигация месяцами ◀/▶, KPI 3 (рабочих/чел-дней/сумма), Excel-экспорт через `/api/timesheet/global/:y/:m/export`. RBAC просмотра — ADMIN/DIRECTOR_*/TO/HEAD_TO/WAREHOUSE/HR; редактирования — ADMIN/DIRECTOR_* все типы; TO/HEAD_TO только medical; WAREHOUSE только warehouse. Клик по пустой ячейке (editable) → TypePickerModal с grid-кнопками → PUT `/api/timesheet/global/entry`. Auto-refresh каждые 30с. Backend возвращает `employees[].works[].cells[]` (vanilla ожидала `workers[].entries` — устаревшая) — адаптер flatten + groupByWork. |

## Группа 7 — Коммуникации / Хугинн (11 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/chat` | chat_groups.js | 1 851 | ✅ | `pages/Chat/index.jsx`. Хугинн (групповые/личные чаты). |
| `/messenger` | (тот же) | — | ✅ | Алиас на `/chat`. |
| `/mailbox` | mailbox.js | 993 | ✅ | Общий почтовый ящик организации (RBAC: ADMIN/DIRECTOR_*/HEAD_TO). 3-pane layout: sidebar папок (📥 Входящие/⭐ Избр/📤 Отпр/📝 Черн/📦 Архив/🗑 Корзина с бейджами + по типу + per-account фильтр) + список (поиск + SelectInput типов + чек-боксы + bulk-bar mark_read/archive/delete/spam + пагинация по 50) + детализация (`EmailDetail.jsx`: reply/reply_all/forward через `ComposeModal.jsx`, ⭐ toggle / 📦 archive / 🗑 delete через PATCH /:id, AI-секция inbox_application с цветной плашкой green/yellow/red + 6 действий accept/reject/review/reanalyze/archive + создать заявку из письма, sandboxed iframe для HTML тела, цепочка thread, вложения с download). Endpoints: GET `/emails`, `/emails/:id`, `/stats`, `/accounts`; PATCH `/emails/:id`; POST `/emails/bulk`, `/send`, `/api/inbox-applications/*` (from-email/accept/reject/review/analyze/archive). Без `window.confirm/prompt/alert`. **Coverage 100% (6/6 endpoints, 3/3 modals).** |
| `/my-mail` | my_mail.js | 1 776 | ✅ | `pages/MyMail/index.jsx`. Моя почта (IMAP+SMTP). |
| `/mail-settings` | mail_settings.js | 614 | ✅ | Настройки почты (RBAC: ADMIN + DIRECTOR_GEN). 4 таба: 1) Аккаунты IMAP/SMTP — список с тестом подключения (синхр./правка/удаление), `AccountEditModal` с PasswordInput/Switch/тест IMAP+SMTP; 2) Правила классификации — таблица с типом/паттерном/режимом/классификацией/уверенностью/приоритетом/совпадениями, `RuleEditModal` + `TestClassifyModal`; 3) Шаблоны писем — `TemplateEditModal` (CRUD + use_letterhead + default_cc + category); 4) Лог синхронизации — таблица с цветными pill-статусами. Endpoints `/api/mailbox/accounts(/test-imap\|test-smtp\|/:id/sync)`, `/classification-rules(/test\|/:id)`, `/templates(/:id)`, `/sync-log`. **Coverage 100% (11/11 endpoints, 3/3 modals).** |
| `/correspondence` | correspondence.js | 857 | ✅ | Реестр входящих и исходящих документов (RBAC: ADMIN/DIRECTOR_*/OFFICE_MANAGER/PM/HEAD_PM/TO/HEAD_TO). 3 KPI карточки (всего/incoming/outgoing с цветными pre-полосами), 5 фильтров (год по умолч. tek + 4 пред / месяц / направление / тип документа из 9 / поиск по теме+контрагенту+номеру), таблица с группировкой 2-токовый по направлению + цветная боковая полоса. `CorrFormModal` (CRUD: дата / номер — для исходящих авто-preview через `/api/correspondence/next-outgoing-number`, для входящих ручной; doc_type 9 типов; subject required; counterparty/contact_person; **привязка к customer/tender/work через Combobox** — новое; FileDrop с upload и привязка через `/link-doc`; **🧙 Мимир-кнопка** через `/api/mimir/suggest-form` для автозаполнения subject/note/counterparty/contact_person). `CorrViewModal` — карточка с KV-таблицей + ссылки на привязанные сущности + 📥 скачать вложение + ✎ Edit / 🗑 Delete через ConfirmModal. Deep-link `?id=`. Backend: POST `/api/correspondence` (allocate номер outgoing), PUT `/api/correspondence/:id`, GET `/api/data/correspondence` (generic CRUD), POST `/api/correspondence/:id/link-doc`, POST `/api/files/upload`. **Coverage 100% (5/5 endpoints, 5/5 modals).** |
| `/telephony` | telephony.js | 2 280 | ✅ | `pages/Telephony/index.jsx`. Mango: история звонков, запись, маршрутизация. |
| `/call-reports` | call_reports.js | 588 | ✅ | `pages/CallReports/{index,api,ReportDetailModal,GenerateModal,ScheduleModal}`. Список отчётов Mango + дашборд + расписание. Endpoints `/api/call-reports/*`. RBAC: ADMIN, DIRECTOR_*. |
| `/mango` | (вкл. в telephony) | — | ✅ | Алиас: `<Navigate to="/telephony" replace />`. |
| `/integrations` | integrations.js | 488 | ✅ | `pages/Integrations/{index,api,BankTab,PlatformsTab,ErpTab,integrations.css}`. 3 таба через `TabsBar`. **Bank/1C**: 4 KPI из `/bank/stats` (доходы/расходы/баланс/неразнесённых), фильтры (статус/направление) + поиск, таблица транзакций с чекбоксами + цветными pill-статусами. `UploadModal` (FileDrop CSV/TXT → POST `/bank/upload` multipart, формат+stats). Экспорт в 1С (`/bank/export/1c?token=`). Правила классификации. **Platforms**: KPI + блок дедлайнов <7д, фильтр платформ + поиск, карточки с цветной точкой по AI-релевантности (≥70/40), `DetailModal` (поля + AI-анализ + кнопка «Создать заявку» → POST `/platforms/:id/create-pre-tender`). «Обработать новые письма» → `/platforms/parse-batch`. **ERP**: список подключений (`/erp/connections`) с иконками 1С/SAP/Galaxy/Custom, тест+экспорт, `AddModal`+`ExportModal`, журнал sync-log. RBAC: ADMIN/BUH/DIRECTOR_*/HEAD_TO/TO. Без window.*. |
| `/telegram` | telegram.js | 410 | ⏳ | Legacy fallback `<LegacyRedirect route="/telegram"/>` → `/legacy/#/telegram`. Vanilla хранит настройки в IndexedDB+`AsgardDB.put('settings','telegram',…)`. Полная миграция требует серверной части (миграция, роут `/api/integrations/telegram`, RBAC); UI без бэка бесполезен. Вне рамок UI-аудита. |

## Группа 8 — Аналитика / Хроники (8 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/analytics` | — | — | ✅ | Алиас: `<Navigate to="/dashboard" replace />`. Отдельной vanilla-страницы нет. |
| `/kpi-works` | kpi_works.js | 575 | ✅ | `pages/KpiWorks/index.jsx`. KPI по работам — комбинированная оценка по РП (себестоимость 35% + сроки 35% + конверсия 30%). Источник `/api/works?limit=2000` + `/api/tenders`. Grade S/A/B/C/D/F. RBAC: ADMIN/HEAD_PM/DIRECTOR_*. |
| `/kpi-money` | kpi_money.js | 462 | ✅ | `pages/KpiMoney/index.jsx`. KPI по деньгам — собираемость и оборачиваемость. Источник `/api/invoices` + `/api/works`. RBAC: ADMIN/BUH/DIRECTOR_*. |
| `/pm-analytics` | pm_analytics.js | 154 | ✅ | `pages/PmAnalytics/index.jsx`. SelectInput год (этот/−1/−2/все) + ↻ Обновить. 6 KPI отдела (всего работ/активных/завершено/просрочено/сумма контрактов/прибыль). Таблица KPI по РП (имя+роль / всего / активных info / завершено ok / просрочено err / контракты / прибыль с зелёным/красным по знаку). Динамика 12 месяцев — бары total_contract с накладкой ok-сегмента (completed/total). Endpoint: GET `/api/works/analytics/team[?year=YYYY]`. RBAC: ADMIN/HEAD_PM/DIRECTOR_*. |
| `/object-map` | object_map.js | 664 | ✅ | `pages/ObjectMap/{index.jsx,object-map.css}`. Leaflet+MarkerCluster (ленивая загрузка CDN — CSS+JS подключаются один раз при первом монтировании, persistent через `leafletLoadingPromise`). 2 tile-провайдера: dark=CARTO voyager / light=OSM standard, переключение по `useTheme()`. Тулбар: 5 чипов-фильтров (Все / 🟢 Активные / 🟡 В тендере / ⚪ Завершённые / 🔴 Без координат) с счётчиками + кнопки `+ Объект` и `📋 Список`. Круговые маркеры по статусам (active/tender/done/pending/unknown), popup (имя/заказчик/работ/активных) + tooltip, кластеризация (small/medium/large). Клик на маркер → `SiteDrawer` (досье): карточка-шапка (статус+название+заказчик+регион+адрес), 4 KPI (работ/активных/тендеров/контракты), геокод-warning со ссылкой «Указать», работы по годам с иконками статусов + клик→`#/pm-works?id=`, тендеры с клик→`#/tenders?id=`, в подвале «Работы/Тендеры» (фильтр по site_id). `AddSiteModal` (MCard): name/customer/SelectInput type (7 типов)/address → POST `/api/sites/geocode` → POST `/api/sites` (highConfidence→auto, found→pending+ручная привязка, не найдено→pending). **Ручная привязка**: editStateRef.mode=true, курсор crosshair, клик на карте → reverse-geocode Nominatim → PUT `/api/sites/:id` + удаление tmp-маркера. `ListDrawer`: поиск, отсортированный по активным работам список с цветной точкой, клик → setView + openSiteDrawer. Endpoints: GET `/api/sites`, GET `/api/sites/:id` (site+works+tenders), POST `/api/sites`, PUT `/api/sites/:id`, POST `/api/sites/geocode`. Drawer открывается через `useModal().open(..., {shape:'drawer-right'})`. RBAC: ADMIN/DIRECTOR_*/HEAD_*. |
| `/meetings` | meetings_page.js | 609 | ✅ | `pages/Meetings/{index,api,MeetingEditModal,MeetingDetailModal}`. Список совещаний + KPI + RSVP + протокол + создание задач из протокола. Endpoints `/api/meetings/*`. |
| `/reminders` | reminders.js | 525 | ✅ | `pages/Reminders/{index,api,ReminderEditModal}`. Список напоминаний с фильтрами (актив/выполнено) + 5 типов (deadline/invoice/work/tender/custom) + 4 приоритета. Backend `/api/data/reminders` (generic CRUD). |
| `/gamification-dashboard` | gamification-dashboard.js | 147 | ✅ | `pages/GamificationDashboard/index.jsx`. Директорский дашборд геймификации — KPI монет/спинов/призов/активных + топ призов + топ рабочих + последние операции. GET `/api/gamification/admin/dashboard`. RBAC: ADMIN/DIRECTOR_*/HR/HEAD_PM. |

## Группа 9 — Документы (5 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/contracts` | contracts.js | 1 332 | ✅ | `pages/Contracts/{index,api,ContractEditModal,contracts.css}`. CRUD через `/api/data/contracts` (generic). KPI 5 (всего/действуют/истекают-30д/истекли/сумма действующих). Фильтры: поиск (номер/предмет/контрагент), тип (customer/supplier), статус (computed). Таблица с computed-статусом (active/expiring/expired/terminated/draft через `computeStatus`). `ContractEditModal`: номер+тип+контрагент (SelectInput из `/api/customers`)+предмет+даты (DatePicker, валидация end>=start)+бессрочно (Switch)+сумма (MoneyInput)+ответственный+статус (draft/active/terminated)+file_url+комментарий, динамическая подсказка по сроку (Истёк/Истекает через N дн./Осталось). RBAC просмотра: ADMIN/PM/HEAD_PM/BUH/OFFICE_MANAGER/директора; удаление — только ADMIN. Эмит `asgard:contracts:changed`. |
| `/seals` | seals.js | 527 | ✅ | `pages/Seals/{index,api,SealEditModal,SealTransferModal,SealHistoryModal,seals.css}`. CRUD `/api/data/seals` + `/api/data/seal_transfers`. KPI 5 (всего/в офисе/у сотрудника/передаётся/утеряно). Фильтры: поиск (название/инв.№), тип (8 видов), статус. `SealEditModal`: название+тип+инв.№+дата покупки+комментарий. `SealTransferModal`: передача сотруднику или возврат в офис (Switch). Передача — создаёт seal_transfers(status=pending) + апдейтит seal(status=transfer, holder_id, pending_transfer_id). Возврат — апдейтит seal(status=office, holder_id=null). `SealHistoryModal`: журнал передач, кнопка «✓ Принять» для получателя — апдейтит transfer(status=confirmed, confirmed_at) и seal(status=employee). Действия со строкой: 🔄 передать / 📋 история / 🏢 вернуть (если employee/transfer) / 🚨 отметить «утеряна» (ConfirmModal danger) / ✎ редактировать / 🗑 удалить (ADMIN). RBAC: ADMIN/DIRECTOR_*/OFFICE_MANAGER. |
| `/proxies` | proxies.js | 688 | ✅ | `pages/Proxies/{index,api,ProxyTypePickerModal,ProxyEditModal,proxies.css}`. CRUD `/api/data/proxies`. 7 шаблонов доверенностей (general/receive_goods/representation/construction/vehicle/bank/common) — каждый со своим набором полей (FIO/passport/powers_general/description/address/supplier/goods_list/vehicle_brand/vehicle_number/vin/bank_name/account_number). KPI 5 (всего/действуют/истекают-30д/истекли/отозваны). Фильтры: поиск (ФИО/номер), тип, статус (computed). Computed-status (active/expiring/expired/revoked) на основе valid_until. `ProxyTypePickerModal`: сетка карточек 7 типов. `ProxyEditModal`: номер+даты+поля шаблона (textarea для powers_general/description/goods_list/passport). Действия: ✎ редактировать / 📄 скачать .doc (генерация HTML→Blob `application/msword`) / ⛔ отозвать (status=revoked) / 🗑 удалить (ADMIN). RBAC: ADMIN/DIRECTOR_*/OFFICE_MANAGER. Эмит `asgard:proxies:changed`. |
| `/pass-requests` | pass-requests-page.js + backend pass_requests.js | 200+349 | ✅ | `pages/PassRequests/{index,api,PassRequestEditModal,pass-requests.css}`. Endpoints: GET `/api/pass-requests`, GET/POST/PUT/DELETE и PUT `/:id/status`. KPI 6 (всего/черновики/поданы/одобрены/отклонены/выданы). Фильтры: поиск (объект/контакт/работа), статус. `PassRequestEditModal`: заказчик (SelectInput из `/api/customers`) + связанная работа (SelectInput из `/api/works`) + объект + период (DatePicker валидация to>=from) + контактное лицо/телефон + 3 динамических списка: 👷 рабочие (выбор из справочника `/api/staff/employees` или ввести вручную — ФИО+ИНН+паспорт+должность), 🚚 транспорт (марка+гос.номер), 📦 оборудование (название+серийник) + примечания. При выборе сотрудника из справочника — автозаполнение ФИО/паспорта/должности. Кнопки модалки: «➕ Создать черновик» / «💾 Сохранить» / «📨 Подать» (для draft → меняет статус). Действия со строкой: 📄 PDF (через `?token=`), ✎ открыть, 📨 подать (draft→submitted, ConfirmModal), ✅ одобрить (submitted→approved, ConfirmModal success), ❌ отклонить (submitted→rejected, PromptModal с причиной), 🪪 выдан (approved→issued, ConfirmModal), 🗑 удалить (ADMIN, только draft). Deep-link `#/pass-requests?id=123`. WRITE-роли: ADMIN/PM/HEAD_PM/TO/HEAD_TO/HR/HR_MANAGER/DIRECTOR_GEN. |
| `/pm-consents` | pm_consents.js | 95 | ✅ | `pages/PmConsents/index.jsx`. Запросы согласия РП (override по пересечению сроков). PM видит свои → ✓ Согласен / ✕ Не согласен. ADMIN/директора видят все. |
| `/official-employees` | official_employees.js | (см. кадры) | ✅ | См. Группу 6 (Дружина) |

## Группа 10 — Геймификация / Академия (5 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/office-academy` | office_academy.js | 584 | ✅ | Залы Асгарда. index/api/LessonBlocks/LessonModal/QuizModal/LeaderboardModal. GET /api/office-academy/lessons → сетка карточек с обложкой (cover_color+cover_icon), фильтры по трекам (pm/hr/finance/procurement/management/all), поиск. Личная статистика (свитки/пройдено/обязательные/XP/ранг с иконкой 👑⚔️🛡️📜). LessonModal: полный рендерер 15+ типов блоков (cover/intro/text_block/icon_grid/steps/fact_card/heading/list/highlight/warning/quote/scenario/checklist/stat/divider/image/video), heartbeat каждые 30с для накопления read_time_seconds, кнопки «✓ Прочитал»/«Перечитал заново»/«⚔️ Начать испытание». QuizModal: радио-кнопки → submit → разбор ответов с подсветкой correct/wrong + объяснения + XP-бейдж + кнопка «Перечитать свиток» при needs_reread. LeaderboardModal: топ-20 с медалями. Все эндпоинты: /lessons, /lessons/:id, /lessons/:id/heartbeat, /lessons/:id/complete, /lessons/:id/quiz, /leaderboard. |
| `/gamification-leaderboard` | gamification-leaderboard.js | 366 | ✅ | Зал Одина (рейтинг рабочих + турнир). `pages/GamificationLeaderboard/{index,api,gamification-leaderboard.css}`. TopActionsBar + TabsBar. 5 KPI (воины/руны/XP/смены/лидер). Сортировка по 4 метрикам. Подиум 1-2-3. Таблица топ-N с прогресс-баром, медалями для топ-3, rank-pill, streak. Турнирная сетка 4 раунда с подсветкой победителей. GET `/api/gamification/admin/leaderboard`. RBAC: ADMIN/PM/HEAD_PM/HR/DIRECTOR_*. |
| `/gamification-admin` | gamification-crud.js | 616 | ✅ | Кузница геймификации — 4 таба. `pages/GamificationAdmin/{index,api,ShopItemModal,QuestModal,gamification-admin.css}`. Шапка-сводка (GET `/api/gamification/admin/dashboard`). **Товары** CRUD `/api/gamification/crud/shop-items` (карточки + ShopItemModal). **Рулетка** `/prizes` (таблица + `/prizes/:id/toggle`). **Квесты** CRUD `/quests` (QuestModal). **Победители** `/winners?status=&from=&to=` (фильтры, 4 KPI, действия «📦 Готово» и «✅ Выдать» через PromptModal → PUT `/winners/delivery/:fid`). RBAC: ADMIN/DIRECTOR_GEN/OFFICE_MANAGER/HR. |
| `/pm-prizes` | pm-prizes.js | 451 | ✅ | Призы воинов — запросы и история. `pages/PmPrizes/{index,api,pm-prizes.css}`. 3 KPI. Табы Запросы/История. **Запросы**: группировка по воину с раскрытием, поиск ФИО, чипы статуса (all/requested/pending) + категории, pulse-анимация на urgent. По строке «⚔️ Выдать» → PromptModal с заметкой → PUT `/api/gamification/admin/inventory/:id/deliver`. **История**: выданные призы. Endpoints: GET `/pending-deliveries`, `/delivered-history`, PUT `/inventory/:id/deliver`. RBAC: ADMIN/PM/HEAD_PM/DIRECTOR_*. |
| `/quests` | (вкл. /gamification-admin) | — | ✅ | Управление квестами — вкладка «⚔ Квесты» на странице `/gamification-admin`. Отдельный URL не нужен. |

## Группа 11 — Система / Кузница (10 страниц)

| URL | Vanilla файл | Строк | Статус | Заметки |
|-----|--------------|-------|--------|---------|
| `/settings` | settings.js | 761 | ✅ | 6 табов (Система/SLA/Калькулятор/Справочники/Компания/Цвета). RBAC: ADMIN+DIRECTOR_*. Чтение/запись через `/api/settings/{app,refs}` (PUT). Калькулятор: textarea JSON для role_rates/chemicals/transport с валидацией. Справочники: textarea построчно, авто-вставка «Черновик»/«Новый» в tender_statuses. Цвета: color-picker + sync с hex-инпутом, по 9+5 статусов. Сброс к дефолту через ConfirmModal. **Полировка 14.06.2026 → 9.5+/10:** В лейблы табов добавлены иконки (⚙️/🛡/🧮/📚/🏢/🎨). Hero-блок «Текущий профиль системы» сверху с компанией, темой, НДС, датой Ганта + meta пользователь/роль; цветной топ-бордер из gold→red→blue. Карточки настроек получили cl золотую полоску слева, lift hover на translateY(-1px) + boost тени, box-shadow:var(--sh-card). Информер-подсказка под табами с описанием раздела (TAB_HINTS). |
| `/system-panel` | (вкл. в settings) | — | ✅ | Системная панель встроена в `/settings` (таб «⚙ Система»). Отдельный URL не нужен. |
| `/diag` | diag.js | 146 | ✅ | Vanilla был на IndexedDB — переписан на реальный `/api/admin/system/{health,updates,logs,action}`. 4 секции: ресурсы (RAM/CPU/Диск с meter-барами и tone по %), сервис+БД (status pills, uptime, Node, active_users), последние релизы (app_updates), журнал journalctl (фильтр all/warn/error). Ping-кнопка, рестарт сервиса (ConfirmModal), бамп SHELL_VERSION. Auto-refresh каждые 15 с. RBAC: только ADMIN. |
| `/backup` | backup.js | 135 | ✅ | Vanilla экспортировал IndexedDB → переделал под серверные pg_dump-бэкапы через `POST /api/admin/system/action` (run-command). Листинг `ls /root/asgard_backup_*.dump` с парсингом, кнопка «＋ Новый pg_dump» (PromptModal — метка для имени), удаление (ConfirmModal danger), памятка по pg_restore. Доп. экспорт настроек в JSON (`/api/settings`) для не-ADMIN директоров. RBAC: дампы — только ADMIN, экспорт настроек — ADMIN+DIRECTOR_*. |
| `/sync` | sync.js | 656 | ✅ | Vanilla синхрила IndexedDB ↔ внешний REST — пересобран под ERP-подключения (1С/SAP/Парус/банки) через `/api/integrations/erp/*`. Список подключений с pill-статусом, ConnectionEditModal (CRUD: имя/тип/URL/auth basic/bearer/apikey/oauth2/none, направление, интервал), кнопки Тест (`/test`), Выгрузить (`ExportModal` с entity_type payroll/bank/tenders/counterparties + период), Править, Деактивировать. Справа — журнал sync-log (`/erp/sync-log`) + банковские выписки (`/bank/batches`) + KPI доходов/расходов (`/bank/stats`). RBAC: ADMIN, директора, BUH. |
| `/mail-settings` | mail_settings.js | 614 | ✅ | 4 таба (Аккаунты/Классификация/Шаблоны/Лог). **Аккаунты**: AccountEditModal (IMAP+SMTP+sync, тесты через `/test-imap`/`/test-smtp`, ручной sync через `/sync`). **Правила**: таблица с pill-классификацией + RuleEditModal (тип/режим/паттерн/класс/уверенность/приоритет) + TestClassifyModal (тестовое письмо → `/test`). **Шаблоны**: список с категориями + TemplateEditModal (code/name/category/subject/body/letterhead/CC). **Лог**: таблица sync-log из `/api/mailbox/sync-log`. RBAC: ADMIN+DIRECTOR_GEN (SETTINGS_ROLES). Все endpoints: `/api/mailbox/{accounts,classification-rules,templates,sync-log}`. |
| `/user-requests` | — | — | ✅ | `pages/UserRequests/index.jsx`. Заявки пользователей (для ADMIN). |
| `/mimir` | mimir.js | 1532 | ✅ | Глобальный FAB-чат Мимира (не страница, виджет в `<HashRouter><MimirFab /></HashRouter>`). `widgets/Mimir/{MimirFab.jsx,api.js,mimir.css}`. Скрыт на /welcome, /login, mobile (<768px), без user. FAB справа-снизу с руной ᛗ, click → панель с шапкой (avatar/имя/статус), sidebar диалогов (GET `/api/mimir/conversations`), сообщений (user/assistant с markdown: code-blocks/inline-code/headers/bold/lists/links/tables/HR), welcome-чипы из GET `/api/mimir/suggestions`, attachments (drag&drop, до 3 файлов 20MB). Отправка: chat-stream (SSE-like) с автоматическим fallback на `/chat`. Файлы → `/analyze` (multipart). Hash-контекст (тендер/работа/финансы/персонал) подмешивается. Hash-links в ответах открывают SPA-навигацию. Wisdom-строка снизу. RBAC: любой залогиненный, кроме mobile/welcome. |
| `/command-map` | command-map.js | — | ✅ | `pages/CommandMap/index.jsx`. Карта команд. |
| `/welcome` | welcome.js | (в app.js) | ✅ | `pages/Welcome/index.jsx`. Главная-приветствие с фирменной эмблемой. |
| `/more` | — | — | ✅ | `pages/More/index.jsx`. Меню «Ещё» (для мобильного-стилевого нижнего меню). |

## Группа 12 — V2-каталог (новые)

| URL | Файл | Статус | Заметки |
|-----|------|--------|---------|
| `/modals` | pages/Modals.jsx | ✅ | каталог 85 компонентов UI 2.0 |

## Группа 13 — Field PWA (~30 страниц, ~9 000 строк)

| URL | Vanilla файл | Статус |
|-----|--------------|--------|
| `/field/home` | field-home.js | ⏳ |
| `/field/shift` | field-shift.js | ⏳ |
| `/field/checkin` | field-checkin.js | ⏳ |
| `/field/earnings` | field-earnings.js | ⏳ |
| `/field/shop` | field-shop.js | ⏳ |
| `/field/quests` | field-quests.js | ⏳ |
| `/field/academy` | field-academy.js | ⏳ |
| `/field/profile` | field-profile.js | ⏳ |
| `/field/team` | field-team.js | ⏳ |
| `/field/pm-*` | field-pm-*.js | ⏳ |
| ... 20+ др. | ... | ⏳ |

> **Field PWA — отдельный модуль.** Зелёный/жёлтый стиль, мобильный-first, оффлайн. Решение: либо отдельный `/v2-field/`, либо отложить до конца десктопа.

---

## Дорожная карта по сессиям

| Сессия | Что делается |
|--------|--------------|
| 1 (предыдущая) | Каталог UI 2.0, эталон Home, виджеты, сайдбар |
| 2 (текущая) | Hover-меню, аудит API, каркас Tenders, журнал |
| 3 | Tenders — Editor wizard + остальные модалки + 3 панели |
| 4 | PmWorks полностью |
| 5 | Field PWA (через отдельных агентов) |
| 6-10 | Волны 1-4 агентов на оставшиеся ~115 страниц |

---

## Лог изменений

| Дата | Что добавлено |
|------|---------------|
| 2026-06-14 | Создан журнал. Каркас Tenders (5 файлов). Hover-меню сайдбара. Аудит 24 API endpoint, фикс 6 виджетов. |
| 2026-06-14 | **Tenders полная версия:** TenderEditor wizard (3 шага) + 6 status-модалок + 3 панели. Расширил Combobox для async (onQuery/allowFreeText). |
| 2026-06-14 | **PmWorks полная версия:** index/filter/list/row + WorkDetailModal (финансы) + 5 stub-модалок интеграций + FieldTabModal с 10 вкладками (3 реальные + 7 заглушек). 123 модуля, 452 KB. |
| 2026-06-14 | **🚫 ЗАПРЕТ ЗАГЛУШЕК (правило в памяти).** Все 7 заглушек FieldTab → реальные с CRUD. WorkDetail: блок «Связанные модули» → меню «⚡ Действия» (15 кнопок, все рабочие). Новые модалки: WorkHistory, Invoice, Act, CloseoutWizard (4 шага), MimirActuals. Расширил Combobox для async. ActionGridModal — универсальный grid-меню. 130 модулей, 496 KB. |
| 2026-06-14 | **Funnel + Customers полные версии.** `/funnel`: канбан 10 стадий с HTML5 drag&drop, 5 модалок переходов (Archive/Lost/Won/Unarchive/Backward), KPI-стрип, клик→TenderEditorModal, серверная карта прав /transition-map. `/customers`: список + поиск + фильтр + пагинация; CustomerEditModal с CRUD и ДаДата-lookup по ИНН; CustomerDetailModal со светофором и KPI из /dashboard и последними тендерами; роуты `#/customers?inn=…` открывают карточку. Расширил Blocks: TopActionsBar/TabsBar/EmptyState теперь принимают props без поломки каталога. 158 модулей, 577 KB. |
| 2026-06-14 | **AllWorks + AllEstimates полные версии.** `/all-works`: Свод Контрактов для руководства, GET `/api/works?limit=1000` (PM на бэке фильтруется; HEAD_PM/DIRECTOR_*/ADMIN видят всё), фильтры период/РП/статус/поиск, сортировка по 6 колонкам с пагинацией, EmptyState на нулевой выборке, клик по строке → реюз `WorkDetailModal` из PmWorks (15-кнопочное меню «⚡ Действия»), кнопка экспорта в CSV (UTF-8 BOM, ';' разделитель). `/all-estimates`: Свод Расчётов, GET `/api/estimates`, фильтры период/РП/статус/поиск, EstimateDetail-модалка с полями (заказчик/РП/версия/Цена ТКП/Себестоим./отправлено/решение/cover_letter/комментарии) и 4 действиями согласования (POST `/api/approval/estimates/:id/{approve,rework,question,reject}` через PromptModal с обязательным комментарием) + кнопка resubmit для PM при rework/question, deep-link `#/all-estimates?id=`. Кнопка «Очередь согласований» → legacy `/approvals`. 158 модулей, 547.94 kB. |
| 2026-06-14 | **OfficeAcademy + TrainingBoard полные версии.** **`/office-academy`** (Залы Асгарда): `pages/OfficeAcademy/{index,api,LessonBlocks,LessonModal,QuizModal,LeaderboardModal,office-academy.css}`. Сетка карточек уроков с обложкой (cover_color фон + cover_icon), 5-чиповая статистика пользователя (свитков доступно / пройдено / обязательные не сданы / XP / ранг 👑⚔️🛡️📜 цветной). Фильтры по трекам (pm/hr/finance/procurement/management/all → TRACK_LABELS+TRACK_COLORS) + поиск по title/saga. LessonModal: heartbeat каждые 30с (skip если document.hidden), полный рендерер 16 типов блоков из БД blocks JSONB (cover/intro/text_block/icon_grid/steps/fact_card/heading/subheading/text/list/numbered/highlight/warning/quote/scenario/checklist/stat/divider/image/video с YouTube embed); кнопки переключаются по состоянию (read_completed_at, attempts, needs_reread): «✓ Прочитал» (POST /complete с MIN_READ_SECONDS=60 проверкой), «Прочитал заново» при needs_reread (сброс попыток), «⚔️ Начать испытание». QuizModal: 2 экрана в одной модалке (форма с радио → submit → разбор с correct/wrong подсветкой опций + 💡 explanation + XP-бейдж +60/+30 + кнопка «📖 Перечитать»). LeaderboardModal: топ-20 с медалями 🥇🥈🥉, ранги цветные. **`/training-board`** (Обучение и допуски): `pages/TrainingBoard/{index,api,DetailModal,CompleteModal,training-board.css}`. RBAC ADMIN/TO/HEAD_TO/DIRECTOR_GEN. GET /api/training/pending → таблица (рабочий+должность, объект, не хватает допуска+training_type, дедлайн с overdue/<7д бейджами, статус, ссылка на сертификат через ?token=, действия). Сводка: ожидает/в процессе/просрочено/<7д. Поиск + фильтр статусов. CompleteModal: multipart upload через fetch+FormData → PUT /:id/complete (certificate_number/valid_from/valid_to/trainer_name → бэк сам INSERT в employee_permits если есть permit_type_id+valid_to). DetailModal: полная карточка обучения + ссылка на скачивание. Все toast/PromptModal/AlertModal — без window.alert/confirm. Билд: 192 модуля, 688.34 KB JS / 196.15 KB gzip, 0 warning. |
| 2026-06-14 | **Gantt (3 роута) + Approvals + ApprovalPayment полные версии.** **`/gantt-calcs|works|objects`**: ОДИН компонент `pages/Gantt/index.jsx` определяет `kind` через `useLocation()` (path-substring). `pages/Gantt/GanttChart.jsx` — свой DOM-рендер шкалы недель (без библиотек: shapка-grid + строки-track + бар `position:absolute, left/width:%`). Линия «сегодня», обрезка баров за пределами окна (gantt-bar--cut-left/right с пунктиром), tooltip с диапазоном. Фильтры: поиск, тип (objects), активные/lost/done, РП-select, статус, период (custom/month/year/last12/all), масштаб (12/26/52/104 нед), date-range inputs. RBAC: !isDir видит только свои (responsible_pm_id/pm_id). Клик на тендер → `#/tenders?open=`, на работу → реюз `WorkDetailModal` из PmWorks. Свой `gantt.css` (стили в темовых vars). **`/approvals`**: страница для директоров и HEAD_TO. Карточки просчётов с KPI (цена/себестоим/маржа% — окрашена по тону), фильтры (поиск/режим sent vs all/РП), пагинация 25 на стр. `EstimateApprovalModal` — лоадит /api/estimates/:id с calculation+documents+huginn, рендерит контекст тендера/v/sent + 4 KPI + payment_terms + cover_letter (collapsible) + список документов как pills + ленту approval_comments (live POST новые). 4 действия: ✓ approve без комментария, 🔄 rework / ❓ question / ✗ reject — через PromptModal-обязательный комментарий. Endpoint POST `/api/approval/estimates/:id/{approve,rework,question,reject}` + GET/POST `/api/approval/estimates/:id/comments`. **`/approval-payment`**: страница для BUH. Hero-pill с балансом кассы. Список заявок с amount, типом сущности, payment_status-pill. `PaymentModal` — 2 опции-карточки (💳 ПП с комментарием → /pay-bank, 💵 наличные с суммой+комментарием → /issue-cash с client-валидацией баланса). Кнопки «🔄 На доработку» и «❓ Вопрос» через PromptModal. Лента approval_comments. После любого действия — `window.dispatchEvent('asgard:approval-payment:changed')` + refresh. Build: 170 модулей, 616.74 kB JS (176.58 KB gzip). 0 warnings. |
| 2026-06-14 | **OfficeSchedule + Travel + OfficialEmployees + GlobalTimesheet полные версии (Группа 6 — Дружина).** **`/office-schedule`**: `pages/OfficeSchedule/{index,api,StatusPickerModal,office-schedule.css}`. Календарная сетка месяц × сотрудники с 10 статусами (оф/уд/об/бн/сс/км/пг/уч/ск/вх). Sticky-колонка ФИО + sticky-шапка дней, выходные подсвечены (вс/сб), сегодня — золотой бордер. Клик по ячейке → StatusPickerModal (SelectInput). Сид staff из активных users (без BOT/ADMIN/FIELD_WORKER/test_/mimir_bot). Все CRUD через универсальный `/api/data/{staff,staff_plan,users,settings}`. RBAC: сотрудник редактирует только свою строку; OFFICE_MANAGER — любую; ADMIN/директора — если `settings.app.schedules.office_strict_own=false`. Без `window.confirm/prompt/alert`, без AsgardUI. **`/travel`**: `pages/Travel/{index,api,TravelAddModal,TravelUploadModal,travel.css}`. KPI 4 (всего/сумма/отправлено/ожидают), 4 таба (Билеты/Жильё/Направления МО/Обучение), SearchInput, грид карточек. Создание через `/api/field/logistics` POST (тип/сотрудник/маршрут/datetime-local вылет-прилёт/№ рейса/датыС-По/сумма+НДС/проект/файл) — бэк автосоздаёт travel-stage для билетов и расход в work_expenses. Действия: 📎 Файл (TravelUploadModal с multipart), ✅ Куплено (POST `/:id/purchased`), 📨 Отправить (POST `/:id/send` → SMS Mango + Push notification), 🗑 (ConfirmModal → DELETE через `/api/data/field_logistics/:id`). RBAC: ADMIN/OFFICE_MANAGER/HR/PM/HEAD_PM/директора/TO. **`/official-employees`**: `pages/OfficialEmployees/{index,api,OfficialEmployeeEditModal,official-employees.css}`. Таблица оклада + несгораемой + статуса + долга компании, KPI 4 пилюли (всего/работают/в отпуске/долг). Просмотр — ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/BUH; редактирование оплаты — ADMIN/DIRECTOR_GEN/BUH; **редактирование ПД (паспорт/ИНН/СНИЛС/адрес/ДР) — ADMIN/HR/HR_MANAGER/DIRECTOR_GEN** (вкладка «🆔 Паспорт» скрыта от остальных). Модалка с табами 💰 Оплата (оклад/несгораемая/статус + период leave для отпуска/декрета/больничного + переключение тип занятости в самозанятые/обратно через ConfirmModal) и 🆔 Паспорт (lazy-load `/api/staff/employees/:id` при открытии вкладки, серия/№/выдан/дата/код подразделения/ДР/место рождения/адрес рег/ИНН/СНИЛС + WARN-баннер о ПД). Backend: PUT `/api/payroll-dashboard/official-employees/:id` (оплата) + PUT `/api/staff/employees/:id` (ПД). **`/global-timesheet`**: `pages/GlobalTimesheet/{index,api,TypePickerModal,global-timesheet.css}`. Sticky-таблица месяц × рабочие с группами по работам. 6 типов ячеек (Д/Н/🚗/📦/🏥/⏳) с цветами из CELL_TYPES. Навигация месяцами ◀/▶, KPI 3 (рабочих/чел-дней/сумма), Excel-экспорт через `/api/timesheet/global/:y/:m/export` (бэк пока отдаёт JSON, фронт выбирает расширение по content-type). RBAC просмотра — ADMIN/DIRECTOR_*/TO/HEAD_TO/WAREHOUSE/HR; редактирования — ADMIN/DIRECTOR_* все типы; TO/HEAD_TO только medical; WAREHOUSE только warehouse. Клик по пустой ячейке (editable) → TypePickerModal с grid-кнопками → PUT `/api/timesheet/global/entry`. Auto-refresh каждые 30с (раньше 10с в vanilla — увеличил, чтобы снизить нагрузку). Backend возвращает `employees[].works[].cells[]` (vanilla ожидала `workers[].entries` — устаревшая) — адаптер flatten + groupByWork. Билд: 225 модулей, 772.50 kB JS / 220.96 KB gzip, 0 warnings. |
| 2026-06-14 | **Personnel (+ employee) + HrRequests + HrRating полные версии (Группа 6 — Кадры).** **`/personnel` + `/employee`**: `pages/Personnel/{index,api,AddEmployeeModal,EditEmployeeModal,EmployeeDetailModal,ReviewModal,personnel.css}`. Реестр «Дружина»: 5 кликабельных бейджей по статусу (on_site/approved/ready/not_ready/archive — клик переключает фильтр), 3 фильтра (поиск ФИО/телефон + специальность + статус), таблица с группировкой по статусу + документами 🔴(просрочено)/⚠️(скоро)/✅, прогресс-бар СЗ-лимита 2.4М ₽ (3 цвета по %), рейтинг с цветом 8/6/<4. Пагинация по 50. AddEmployeeModal (POST `/api/staff/employees` — fio+phone+birth+spec+grade+city+notes). EmployeeDetailModal (deep-link `#/employee?id=`): KPI карточки (статус+дата/рейтинг/СЗ-остаток/трудоустройство), смена статуса для HR+ADMIN+директоров (ready+дата / not_ready+причина / archive — PUT `/api/staff/readiness/:id/status`), СЗ-блок с прогрессом+5 последних операций из `/readiness/log/:id`, официальное трудоустройство (read-only), **🔒 PII блок (паспорт/ИНН/СНИЛС/банк/адрес рег) — только HR/ADMIN/директора через `canSeePII()`**, последние 10 оценок. Кнопки: 📋 Анкета (редирект на legacy /employee?id=), ✎ Редактировать, ★ Оценить, 📅 График. EditEmployeeModal — 5 секций (Основное / Работа / 🔒 ПII / 🔒 Банк / Заметки) с PhoneInput/DatePicker/Checkbox/SelectInput. ReviewModal — Slider 1–10 с цветным тоном + Textarea. RBAC: VIEW=HR/PM/HEAD/директора/TO/HEAD_TO/OFFICE_MANAGER, EDIT=HR/ADMIN/директора (PM read-only), PII=EDIT_ROLES. Источник: GET `/api/staff/readiness` (effective_status / permits.expired-expiring / se_transferred_year). **`/hr-requests`**: `pages/HrRequests/{index,api,RequestFormModal,PmViewModal,HrSplitModal,hr-requests.css}`. Список заявок персонала v2: PM видит `/api/staff-requests/my`, HR — `/pending`. Фильтр по статусу (8 status_v2) + поиск + опц. фильтр work_id. Hash-парам: `?create=1[&work_id=]` (PM открывает форму) и `?id=` (карточку). **RequestFormModal** (PM создаёт/редактирует): объект из `/api/works?my=1`, 8 позиций (master/fitter/welder/pto/chemist/insulator/assembler/laborer) через NumberInput, даты, условия (питание сухпаёк/столовая/сам + жильё вагон/гостиница/общежитие + ротация), блок «Требуемые допуска по должностям» рендерит для активных ролей + «для всех должностей»: типы из `/api/permits/types`, **«свой допуск»** (категория из 8: special/safety/electric/gas/medical/attest/offshore/transport) через POST `/permits/work/:wId/requirements/custom`, маркер «не требуются», 409-разбор missing_roles от submit. Auto-сейв черновика (POST → PUT `/:id/draft`), submit `PUT /:id/submit`. **PmViewModal** (PM): StatusBadge, прогресс позиций, назначенные рабочие, hr_comment в orange-бейдже, «✎ Редактировать» (draft/rework) и «+ Добавить в бригаду» (approved через ConfirmModal → PUT `/:id/add-to-crew`). **HrSplitModal** (HR подбор, 2-колоночный): авто PUT `/:id/take` при new, слева детали+позиции+назначенные с «✕ Убрать» (DELETE `/assign/:id`), справа `/:id/available-workers?role=` (поиск ФИО + SelectInput-фильтр по ролям, conflict_reason role_mismatch ⚠️ / missing_permits 🔴, ★ рейтинг), «+ Добавить» (PUT `/:id/assign`). Кнопки: 💾 Сохранить и выйти, 📤 sent_to_pm, ✅ approve через ConfirmModal, 🔄 rework через PromptModal с обязательным комментарием. RBAC: PM/HEAD_PM (свои), HR/HR_MANAGER/ADMIN/DIRECTOR_* (входящие). **`/hr-rating`**: `pages/HrRating/{index,api,hr-rating.css}`. Рейтинг рабочих (vanilla читала локальный IndexedDB — в v2 берём rating_avg/rating_count прямо из GET `/api/staff/readiness`). Офисные роли отсеиваются по OFFICE_ROLES. Топ-3 — карточки-трофеи 🥇🥈🥉 с большой пилюлей рейтинга. Таблица: место (с трофеем для 1-3), ФИО, роль, город, рейтинг (цветная точка по 8/6/<4), оценок, последняя оценка. Поиск, пагинация 50. Клик по строке → EmployeeDetailModal (реюз из /personnel). RBAC: ADMIN/HR/HR_MANAGER/директора. Билд: 262 модуля, 894.26 kB JS / 250.50 KB gzip, 0 warnings. |
| 2026-06-14 | **Birthdays + Calendar + Tasks + Alerts полные версии (Группа 1).** **`/birthdays`**: `pages/Birthdays/{index,api,birthdays.css}`. Табы Офис/Рабочие (HR/HR_MANAGER/директор видит обе). Источники: GET `/api/users?is_active=true` (офис, `birth_date`) и GET `/api/staff/employees` (рабочие, `birth_date`, имена `fio`). Утилиты дат `nextBirthday`/`diffDaysUTC`, группировка `buildBirthdayData` → {nearest:12, byMonth:Map}. Сетка карточек «Ближайшие» (today=зелёный/soon≤7д=золотой) + сетка месяцев с подсветкой текущего. Кнопка «Поздравить» → PromptModal с шаблоном → POST `/api/notifications` (только для офисных — у employees нет user_id, toast.info). Hash-sync `?tab=workers`. Кнопки «Уведомления/Настройки SLA/Персонал» — редиректы. **`/calendar`**: `pages/Calendar/{index,api,EventModal,calendar.css}`. Toolbar ‹ Сегодня › + month/week/day switcher. **MonthView**: своя 7-колоночная grid с подсветкой today/weekend, до 3 событий + «+N ещё», клик по дню → создать, клик по событию → редактировать. **WeekView**: 7 колонок (Пн–Вс) с шапкой дней. **DayView**: hero-заголовок «12 июня 2026» + строки `время · название · место · описание`. Sidebar: «Ближайшие 60 дн» (GET `/api/calendar?date_from=&date_to=`) + легенда 6 типов событий с цветами. `EventModal`: CRUD через `/api/calendar` POST/PUT/DELETE; поля title (required), date, time, type (6 опций), location, reminder_minutes (6 опций), description. Удаление через ConfirmModal. **`/tasks`**: `pages/Tasks/{index,api,TaskCard,TaskCreateModal,TodoPanel,tasks.css}`. 2-колоночный layout (≤1100px → 1 колонка): слева задачи, справа sticky todo. Фильтры: поиск + view (Мои/Поручения для директоров) + статус (new/accepted/in_progress/overdue/done). `TaskCard` раскрывается по клику: title+meta (creator/assignee/deadline с overdue ⚠️), статус-pill (5 цветов), priority — левая полоса (urgent/high/normal/low). Раскрытая часть: description, creator_comment, files (download через token), action: Принять (`PUT /:id/accept`), Начать (`PUT /:id/start`), Завершить (PromptModal комментарий → `PUT /:id/complete`), Редактировать (TaskCreateModal с initial), Удалить (ConfirmModal → DELETE). `TaskCreateModal`: assignee (`/api/users?is_active=true`, скрыт при правке) + title + description + deadline (datetime-local) + priority + creator_comment. `TodoPanel`: счётчик done/total, input с Enter → POST `/api/tasks/todo`, чекбокс PUT toggle, double-click → PromptModal edit, ×→ConfirmModal→DELETE. Таймер ⏳ авто-удаления через 48ч. Deep-link `?id=123` — скролл и раскрытие карточки. **`/alerts`**: `pages/Alerts/{index,api,alerts.css}`. Фильтр (Непрочитанные/Прочитанные/Все) + поиск + переключатель группировки (по типу/без). Группировка по `type`: задачи 📋/календарь 📅/согласования ✅/поздравления 🎂/кадры 👥/закупки 🛒/тендеры 📋/рассылки 📢/системные ⚙/сообщения 💬. Сортировка групп: по непрочитанным → по размеру. Действия: открыть (auto-mark-read + переход по link/link_hash), пометить прочитанным (`PUT /:id/read`), удалить (ConfirmModal→DELETE), «✓ Прочитать всё» (`PUT /read-all` с ConfirmModal), «🧹 Удалить прочитанные» (циклом DELETE). Все модалки/toast — через каталог (никаких window.alert/confirm/prompt). **Билд:** 208 модулей, 730.22 kB JS / 208.43 KB gzip, 0 ошибок. |
| 2026-06-14 | **BonusApproval + OneTimePay + SelfEmployed + PmBalance полные версии (Группа 3+4 — Финансы).** **`/bonus-approval`**: `pages/BonusApproval/{index,api,BonusRequestModal,BonusApprovalModal}`. PM создаёт запрос — выбирает работу → подгружает `employee_assignments` + `employees`, сетка рабочих с MoneyInput на каждого + Σ итог + обоснование, POST `/api/data/bonus_requests` со status='pending'. Директор/ADMIN решает в `BonusApprovalModal`: `parseBonuses` tolerant к JSONB/строке/массиву, pill-распределение по рабочим, 4 действия через `/api/approval/bonus_requests/:id/{approve,rework,question,reject}` (rework/question/reject — PromptModal с обязательным комментарием). PM видит только свои (`created_by/pm_id === user.id`), фильтр статуса+поиска, sort: pending первые. **`/one-time-pay`**: `pages/OneTimePay/{index,api,OneTimeCreateModal}`. 5 табов (Все/Ожидают/Согласовано/Оплачено/Отказ), карточки с типом+бейджем+КПИ. CreateModal: Combobox по employees + MoneyInput сумма + SelectInput тип (6: one_time/taxi/fuel/meal/material/other с иконками) + SelectInput работа (PM видит только свои) + TextareaInput причина. Действия: директор → Согласовать (ConfirmModal) / Отклонить (PromptModal с director_comment); BUH+директор → Оплачено (ConfirmModal). Endpoints `/api/payroll/one-time` GET (со status-фильтром) / POST + PUT `/:id/{approve,reject,pay}`. **`/self-employed`**: `pages/SelfEmployed/{index,api,SelfEmployedEditModal,SelfEmployedDetailModal}`. Реестр НПД — поиск + фильтр статуса (active/suspended/closed) + сетка карточек (ФИО / ИНН / телефон / банк / ****последние 4 счёта / ГПХ). EditModal с CRUD (ИНН 12-цифр валидация в INNInput + бэк подтверждает, PhoneInput для +7-маски, дата ГПХ через native datepicker, статус НПД, комментарий). DetailModal: реквизиты + история выплат `/self-employed/:id/payments` + KV-таблица (БИК/карта/№ГПХ/дата) + итог выплачено. Доступ ADMIN/BUH/DIRECTOR_*. Endpoints `/api/payroll/self-employed`. **`/pm-balance`**: `pages/PmBalance/{index,api,PmBalanceDetailModal}`. KPI-полоса (получено/потрачено/на руках/в минусе), таблица «РП × cash_in / se_cash_in / потратил / вернул / на руках» с цветом по балансу (зелёный/красный/нейтральный) + tfoot с итогами. Клик по строке → `PmBalanceDetailModal` с 5 секциями операций: 💵 из кассы (cash_requests), 🔄 от СЗ (se_transfers), 💳 выплаты наличкой (worker_payments), 🧾 расходы (cash_expenses), ↩️ возвраты в кассу (cash_returns). Каждая секция с итогом. Фильтр по месяцу (клиентский, последние 12 месяцев + «Все»). Доступ ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/BUH. Endpoints `/api/payroll-dashboard/pm-balance` + `/:pm_id`. Backend отдаёт `cash_out_expenses/_returns/_salaries` — нормализация в api.js: `cash_out = expenses + salaries`, `cash_returned = returns`. Деталь как модалка (без отдельного URL — функционально эквивалентно `/pm-balance/:pm_id`, оба пути роутятся в один компонент). Все 4 страницы без `window.confirm/prompt/alert`, без AsgardUI, без кастомных CSS-классов — только готовые компоненты (Btn/TopActionsBar/EmptyState/TabsBar/StatusBadge/Pill/SearchInput/SelectInput/MoneyInput/INNInput/PhoneInput/Combobox/TextareaInput/Field/ConfirmModal/PromptModal) и CSS-переменные. Reactive refresh через `window.dispatchEvent('asgard:<page>:changed')`. **Билд:** 262 модуля, 912.23 kB JS / 254.79 KB gzip, 0 ошибок. |
| 2026-06-14 | **Readiness (РП + директор) + Kanban полные версии (Группа 3 — Работы).** **`/readiness`** + **`/readiness-board`** — один модуль `pages/Readiness/{api,Ring,StageDrawer,ReadinessPmPage,ReadinessBoardPage,readiness.css}`, два экспорта `Pm` и `Board`. Обе страницы делят `Ring.jsx` (SVG-кольцо готовности, цвета `var(--ok)`/`var(--amber)`/`var(--err)` по порогам 80/50) и `StageDrawer.jsx` (drawer с 7 этапами + кнопки override через POST/DELETE `/api/work-readiness/:workId/override`). **`/readiness`** — РП-вид: PM видит свои работы в подготовке (`work_status` ∈ {Новая/Подготовка/Мобилизация}), HEAD_PM/директора/ADMIN — все в подготовке (на бэке `loadWorks` пробрасывает `?my=true`, для не-PM фильтрует клиент). Hero (общая средняя + «горящие»: `<60% и старт ≤14 дн`), сетка карточек с кольцом + блокером + днями до старта. Клик → StageDrawer: кольцо/статус/план старта/главный блокер + список этапов (👷 персонал, 🎓 допуска, 🛒 закупки, 📦 сборы, 🎫 билеты, 🏠 жильё, 🚚 логистика) с items.ok/detail и кнопкой override для тех, кто может (PM свои, HEAD_PM/директора/ADMIN любые). **`/readiness-board`** — директорский светофор: 🔴 (есть горящие) / 🟡 (средняя <70%) / 🟢. Группировка работ по `pm_id`, имена РП из `/api/users?role=PM&is_active=true`, hero (общая средняя + всего горящих), поиск по ФИО РП, клик на РП → drawer со списком его проектов (карточки с кольцом), оттуда клик на проект → тот же StageDrawer. RBAC: ADMIN/HEAD_PM/DIRECTOR_*. Источники: GET `/api/work-readiness/summary?ids=…` (батч, кэш 60с на бэке) + GET `/api/work-readiness/:workId` (full для drawer) + GET `/api/works?limit=1000` + GET `/api/users?role=PM&is_active=true`. Никаких `window.confirm/prompt/alert`, только `useModal`/`DrawerModal`/`Btn`/`TopActionsBar`/`EmptyState`/`SearchInput` + CSS-переменные. **`/kanban`** — `pages/Kanban/{index,api,Column,Card,TaskDetailModal,kanban.css}`. 4 колонки (📥 Новые / 🔄 В работе / 👁 На проверке / ✅ Готово) с цветной верхней полосой по тону. HTML5 drag&drop (паттерн `/funnel`): `draggable` карточка, `dragOver/Leave/Drop` на `kb-col-body`, optimistic update + откат при ошибке `PUT /api/tasks/:id/move`. Фильтры: поиск (клиент по title/description/assignee_name), приоритет (low/normal/high/urgent), исполнитель (с пунктом «👤 Мои задачи» в начале) + кнопка «✕ Сбросить». Карточка: цветная левая полоса по приоритету (p-urgent=err, p-high=orange, p-normal=info, p-low=t-3), аватар-инициалы исполнителя, дедлайн с тонами `overdue`/`soon`, счётчик 💬, теги. `TaskDetailModal`: priority+status pill, description (whitespace pre-wrap), KV-ряды (дедлайн/исполнитель/создатель/ознакомление), файлы (`/api/tasks/:id/file/:filename`), лента комментариев (`GET/POST /api/tasks/:id/comments`, system-комментарии курсивом), `TextareaInput` для нового комментария, кнопки: «👁 Подтвердить ознакомление» (`PUT /:id/acknowledge`), «👁 Подписаться/Отписаться» (`POST/DELETE /:id/watch`). Deep-link `#/kanban?id=N` открывает модалку (auto + hashchange). Событие `asgard:tasks:changed` триггерит `refresh()`. RBAC учитывается на бэке (директора видят все, остальные — свои+watchers). **Билд:** 284 модуля, 975.56 kB JS / 271.55 KB gzip, 0 ошибок. |
| 2026-06-14 | **Finances + BuhRegistry + Invoices + Acts полные версии (Группа 4 — Финансы).** **`/finances`**: `pages/Finances/{index,api,PieChart,finances.css}`. Деньги · Аналитика. Табы Расходы/Поступления + переключатель года (2020…tek) + сводка 3 карточек (за год / среднее / максимум). Гистограмма помесячно с clickable bars → клик по столбику открывает детализацию месяца с собственным мини-кольцом. SVG-кольцо без библиотек (`describeArc/polarToCartesian`) по 11 категориям расходов (ФОТ/суточные/наличные/материалы/билеты/проживание/химия/оборуд/субподряд/трансфер/прочее) с легендой+%. Алиасы категорий из vanilla (payroll→fot, chemistry→chemicals, transport→transfer). Источники: `/api/expenses/work?date_from=Y-01-01&date_to=Y-12-31` + `/api/expenses/office?…` + `/api/works?limit=2000` (для advance_date_fact/payment_date_fact). RBAC: ADMIN/BUH/DIRECTOR_*. Кнопка «📄 Импорт выписки» — редирект на legacy `/legacy/#/finances` (модуль AsgardBankImport не мигрирован). **`/buh-registry`**: `pages/BuhRegistry/{index,api,ExpenseViewModal,ExpenseEditModal,buh-registry.css}`. Реестр расходов для BUH. 6 фильтров: год (по умолч. текущий), месяц (по умолч. текущий), заказчик (опции из works), кто внёс (только реально вносившие), категория (12 опций), статус СФ (Нужна/Получена/Не нужна). Сводка 3-6 карточек: всего/ожидают СФ/СФ получены + топ-3 категории по сумме. Таблица: дата, категория (badge с иконкой+цветом через color-mix), сумма, заказчик/работа (с inline ФОТ-детализацией: оклад/сут/премия), статус работы (закрыта/открыта pill), поставщик, № док., СФ-pill (need/got/none), кто внёс. Действия: 👁 Просмотр (read-only с полной ФОТ-секцией) и ✎ Редактировать (только doc_number + invoice_needed + invoice_received, как в vanilla; закрытые работы read-only). CSV-экспорт с BOM + `;` separator. Пагинация 25/стр. Endpoints `/api/expenses/work?limit=10000` + `PUT /api/expenses/work/:id` + `/api/works` + `/api/users`. RBAC: ADMIN/BUH/DIRECTOR_*. **`/invoices`**: `pages/Invoices/{index,api,InvoiceEditModal,InvoiceDetailModal,PaymentModal,invoices.css}`. Счета и оплаты. Список с 6 статусами (Черновик/Выставлен/Ожидает/Частично/Оплачен/Отменён — цветной pill через color-mix), поиск (номер/контрагент/ИНН/описание), фильтр статуса, пагинация 25, сводка `Σ сумма / Σ оплачено` в subtitle. Клик по строке → `InvoiceDetailModal`: лента платежей + кнопки `📄 PDF / 💰 Внести оплату / ✎ / 🗑`. `InvoiceEditModal` (CRUD): авто-номер СЧ-ГГГГ-NNN через GET `/api/invoices/next-number`, выбор контрагента из `/api/customers` (опции по ИНН), привязка к работе через `/api/works`, сумма+НДС%+авто-итого с учётом VAT, due_date, описание. `PaymentModal` (POST `/:id/payments` — бэк сам пересчитывает paid_amount+status, по умолчанию подставляется остаток). Deep-link `#/invoices?id=NN`. PDF открывается в новой вкладке через `?token=` query (бэк подменяет header при отсутствии Bearer). RBAC на запись: ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/PM/BUH. Endpoints: GET/POST/PUT/DELETE `/api/invoices`, `/:id/payments`, `/:id/pdf?token=`, `/next-number`. **`/acts`**: `pages/Acts/{index,api,ActEditModal,ActDetailModal,acts.css}`. Акты выполненных работ. Список с 4 статусами (Черновик/Отправлен/Подписан/Оплачен), поиск, фильтр, пагинация 25. Сводка: count + Σ суммы + Σ подписанных. `ActDetailModal`: карточка + действия `📄 PDF / ✍ Подписан (ConfirmModal success → status=signed + signed_date) / 💰 Оплачен (ConfirmModal gold → status=paid + paid_date) / ✎ / 🗑`. `ActEditModal` (CRUD): авто-номер АКТ-ГГГГ-NNN через `/next-number`, контрагент из `/api/customers`, привязка к работе, amount+vat_pct+авто-total, signed_date, description. Deep-link `?id=NN`. RBAC на запись: ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/PM/BUH. Endpoints: GET/POST/PUT/DELETE `/api/acts`, `/:id/pdf?token=`, `/next-number`. Все 4 страницы — без `window.confirm/prompt/alert`, без AsgardUI/CRSelect, только готовые компоненты из `@/blocks` `@/inputs` `@/modals` (Btn/TopActionsBar/EmptyState/SearchInput/SelectInput/MoneyInput/NumberInput/TextInput/TextareaInput/Checkbox/Field/StatusBadge/ConfirmModal) и CSS-переменные. Reactive refresh через `window.dispatchEvent('asgard:{finances|buh-registry|invoices|acts}:changed')`. **Билд:** 315 модулей, 1105.38 kB JS / 302.98 KB gzip, 0 ошибок. |
| 2026-06-14 | **BigScreen + EngineerDashboard + PmAnalytics + ToAnalytics + ObjectMap (Группы 1 + 8 — Аналитика).** **`/big-screen`**: `pages/BigScreen/{index.jsx,big-screen.css}`. Полноэкранный Command Center — открывается через **`ProtectedBare`** (вне AppShell, без сайдбара/шапки). Занимает весь viewport (`position:fixed; inset:0; z-index:9999`), `body.bs-fullscreen` блокирует overflow страницы. 9 слайдов с авто-ротацией 60с: KPI года (тендеры/конверсия/выручка/работы + команда/заявки/касса/допуски) → Финансовая сводка (контракты/получено/авансы+баланс/активы ТМЦ + касса + SVG-кольцо собираемости) → Воронка тендеров (8 стадий + 6-месячная динамика) → Подготовка проектов (8 карточек-колец, средняя готовность, горящие <60% + ≤14дн) → Руководители проектов (топ-10 по контрактам) → Активные работы (топ-10) → Просроченные (или «всё в графике») → Команда+допуски (бары по ролям + список истекающих) → Заявки+ТМЦ+касса (KPI + воронка статусов кассы). Управление: ←/→/Space/ESC, dots-навигация (`bs-dots`), prev/next кнопки, progress-bar `SLIDE_INTERVAL=60с`, refresh данных каждые 5 мин (`DATA_REFRESH=300с`). Грузит параллельно: `/api/tenders?limit=2000`, `/api/works?limit=2000`, `/api/users?limit=500`, `/api/data/{employees,cash_requests,permits,permit_types}`, `/api/pre-tenders/stats`, `/api/equipment/balance-value`, `/api/works/analytics/team`, потом `/api/work-readiness/summary?ids=...`. SVG `Ring` компонент (без canvas, в темовых vars). Часы обновляются 1 раз/сек через `setInterval`. RBAC: ADMIN/DIRECTOR_*/HEAD_*. **`/engineer-dashboard`**: `pages/EngineerDashboard/index.jsx`. Кузница Инженера. TopActionsBar (↻ Обновить + CTA на склад) + 5 KPI (всего/на складе/на руках у РП/в ремонте/стоимость на руках) + 3 секции с раскладкой: оборудование по РП (toggle-секция per-PM с per-equipment списком: name/inv_number/status/book_value), требуется ТО (30 дн, overdue → красная обводка / soon → жёлтая, holder_name справа), движения за 30 дн (горизонтальные бары по 6 типам: issue/return/repair/write_off/transfer/adjust с цветами через `MOVEMENT_COLORS`). Endpoints: GET `/api/equipment/analytics/by-pm` + `/api/equipment/stats/summary`. RBAC: ADMIN/CHIEF_ENGINEER/DIRECTOR_*. **`/pm-analytics`**: `pages/PmAnalytics/index.jsx`. Хроники РП. SelectInput год (этот/−1/−2/все) + ↻ Обновить. 6 KPI отдела (всего/активные/завершено/просрочено/сумма контрактов/прибыль). Таблица KPI по РП (имя+роль / всего / активных info / завершено ok / просрочено err / контракты / прибыль с зелёным/красным по знаку). Динамика 12 месяцев — горизонтальные бары total_contract с цветной накладкой (`linear-gradient` от ok до info по % completed/total). Endpoint: GET `/api/works/analytics/team[?year=YYYY]`. RBAC: ADMIN/HEAD_PM/DIRECTOR_*. **`/to-analytics`**: `pages/ToAnalytics/index.jsx`. Хроники ТО. SelectInput год + ↻ Обновить. 6 KPI отдела (всего/выиграно/проиграно/в работе/сумма выигр./конверсия). Таблица KPI по тендерным специалистам. Воронка по статусам (горизонтальные бары с автоматическим цветом по regex `/выигр|согласи/i`→ok, `/проигр|откаж/i`→err, иначе info). Динамика 12 месяцев — бары total с накладкой won-сегмента. Endpoint: GET `/api/tenders/analytics/team[?year=YYYY]`. RBAC: ADMIN/HEAD_TO/DIRECTOR_*. **`/object-map`**: `pages/ObjectMap/{index.jsx,object-map.css}`. Карта объектов на Leaflet+MarkerCluster, **лениво загружаемых с CDN** (один раз на сессию через `leafletLoadingPromise` — CSS+JS добавляются в head). 2 tile-провайдера: dark=CARTO voyager / light=OSM standard, переключение через `useTheme()`. Тулбар: 5 чипов-фильтров (Все / 🟢 Активные / 🟡 В тендере / ⚪ Завершённые / 🔴 Без координат) с счётчиками + кнопки `+ Объект` и `📋 Список`. Круговые маркеры (`circleMarker`) по 5 статусам, popup (имя/заказчик/работ/активных) + tooltip, кластеризация (small <10 / medium <100 / large). Клик на маркер → `SiteDrawer` (DrawerModal через `useModal().open(..., {shape:"drawer-right"})`): карточка-шапка (статус+название+заказчик+регион+адрес), 4 KPI-блока, geocode-warning со ссылкой «Указать», работы по годам с иконками статусов + клик→`#/pm-works?id=`, тендеры с клик→`#/tenders?id=`, footer-кнопки «Работы/Тендеры» по site_id. `AddSiteModal` (MCard/MHead/MBody/MFoot): name/customer/SelectInput type (7 типов)/address → POST `/api/sites/geocode` → POST `/api/sites` (highConfidence→auto, found→pending + ручная привязка, не найдено→pending). **Ручная привязка**: `editStateRef.mode=true`, курсор crosshair, клик на карте → reverse-geocode Nominatim → PUT `/api/sites/:id` + удаление tmp-маркера. `ListDrawer`: поиск, отсортированный по активным работам список с цветной точкой, клик → `map.setView` + openSiteDrawer. Endpoints: GET `/api/sites`, GET `/api/sites/:id`, POST `/api/sites`, PUT `/api/sites/:id`, POST `/api/sites/geocode`. RBAC: ADMIN/DIRECTOR_*/HEAD_*. **Билд:** 295 модулей, 1048.64 kB JS / 290.36 KB gzip, 0 ошибок (только информационный warning о размере чанка — нормально для one-bundle Vite SPA). |
| 2026-06-14 | **Procurement + MyProcurement полная версия (Группа 5 — Закупки).** Один компонент `pages/Procurement/{index,api,Kanban,Table,procurement.css}` обслуживает оба роута через проп `mode`: `/procurement` (mode='all' — для PROC/BUH/ADMIN/DIR/HEAD/WAREHOUSE) и `/my-procurement` (mode='my' — для PM/HEAD_PM, server-side фильтр `pm_id=user.id`). Поддерживает hash-параметр `?id=N` (открыть деталь) и `?work=N` (фильтр по работе для прихода из карточки работы). **Канбан** 6 объединённых колонок (🆕 sent_to_proc / 🛠️ proc_responded / ⏳ pm_approved+dir_question+dir_rework / 💳 dir_approved+paid / 🚚 partially_delivered+delivered / ✅ closed+dir_rejected) с HTML5 drag&drop и картой разрешённых переходов по ролям (`kanbanActionFor`: PROC → proc-respond, PM → pm-approve, DIR → dir-approve, BUH → mark-paid, PM/DIR → close). **Таблица** с сортировкой по 8 колонкам и пагинацией. **Дашборд KPI** (pending/overdue/upcoming/paid) — только PROC/ADMIN/DIR на /procurement, клик переключает фильтр статуса. Переключатель Канбан/Таблица в LS `proc_view_v2`. **6 модалок** в `pages/Procurement/modals/`: **ProcurementDetail** (главная, 770 строк) — шапка+мета+позиции с inline-edit (наименование/кол-во/поставщик/цена для PROC, RBAC через `canEditItems`), подсказки цен в строке (батч `POST /price-hints`: last+market avg + кнопка «подставить» для PROC), кнопки действий по статусу+роли (`getActions`), комментарий обязателен для rework/question/reject/return через `PromptModal`, шаблоны (📋 В шаблон через PromptModal), клонирование (🔁 Повторить через ConfirmModal), группировка по категории/поставщику (UI-only `_groupMode`), список загруженных счетов поставщиков (видны бухгалтеру), платёжки, история (timeline). **InvoiceImportModal** — Excel multipart (`POST /:id/invoice/parse` → `parseProcurementExcel` на бэке) / PDF (pdf.js CDN → текст → AI на бэке через routerai) / фото (Tesseract.js CDN рус+eng → текст → AI). Сервер возвращает `{matches:[{item_id,invoice_name,confidence}], unmatched, items_for_match}` с trigram-similarity. UI позволяет вручную привязать unmatched строки к позициям заявки. Применение → `POST /:id/invoice/:importId/apply` с массовой простановкой цен. **SplitItemModal** — разбить позицию на части по поставщикам (`POST /:id/items/:itemId/split` с валидацией `SUM(parts.quantity) == parent.quantity ±0.001`). UI с live-валидацией суммы. **DeliverModal** — приёмка позиций (выбор + опц. ячейка склада → `PUT /:id/items/:itemId/deliver`), визуально checkboxed-cards, progress-bar, финальный экран с золотой подсветкой созданного оборудования. **ShowcaseModal** — витрина каталога (`GET /api/products/catalog-procurement?include_equipment=true&limit=400` → корзина с расчётом «докупить = need − available», нулевые позиции в bulk не уходят → `POST /:id/items/bulk`). **CreateProcurementModal** — title/work/priority/price_segment/budget_limit/notes + опция «Создать из шаблона» (`POST /api/procurement/from-template/:tplId`) + autoShowcase (по умолчанию открывает витрину каталога сразу после создания). **ImportExcelModal** — multipart по шаблону системы (`POST /:id/items/import-excel`). **+ Позиция** (мини-модалка внутри ProcurementDetail) — с подсказкой цены по `GET /api/price-records/hint?name=` при вводе. **Текстовый ввод** позиций через `PromptModal` multiline → `POST /:id/items/import-text` (бэк-парсер `parseTextLine`). **AI-разбор ТЗ** → `POST /:id/items/ai-parse`. **Прикрепление файла-счёта к позиции** через `POST /api/files` (multipart) → `PUT item.invoice_doc_id`. **Цепочка согласования** (все через `PUT /api/procurement/:id/{action}`): send-to-proc → proc-respond → pm-approve / return-to-proc → dir-approve (locked=true) / dir-rework / dir-question / dir-reject → mark-paid → deliver-items → close. RBAC через `getActions(p, role)`. Экспорт реестра/отдельной заявки в Excel (опц. с группировкой по поставщикам). **Coverage audit: 97% (27/27 endpoints + 6/7 модалок)** — последняя «потерянная» модалка `openModal` это базовый `useModal().open` из `@/modals`. Все модалки/toast — через каталог (`useModal/ConfirmModal/PromptModal/AlertModal/toast`), никаких `window.confirm/prompt/alert`, никаких inline-цветов `#fff/#000` — только CSS-переменные `var(--*)`. Использует только готовые компоненты `@/blocks` `@/inputs` `@/modals` (Btn/MCard/MHead/MBody/MFoot/SearchInput/SelectInput/TopActionsBar/EmptyState). 11 файлов, 2948 строк. **Билд:** чистый, vite 6.53s, 426 модулей, 1581.40 kB JS / 425.74 KB gzip, 0 ошибок (только информационный warning о размере общего чанка, что нормально для one-bundle Vite SPA). |
| 2026-06-14 | **Settings + Diag + Backup + Sync + MailSettings полные версии (Группа 11 — Система / Кузница).** **`/settings`**: `pages/Settings/{index,api,settings.css,tabs/{SystemTab,SlaTab,CalculatorTab,RefsTab,CompanyTab,ColorsTab}}`. 6 табов через TabsBar+active. Грузит `GET /api/settings` (`{settings:{app,refs,...}}`) и применяет `deepMerge` поверх `DEFAULT_APP`/`DEFAULT_REFS` (защита от пропусков). System: НДС/Gantt-старт (date↔ISO) /docs-hint/чекбоксы (требовать документы/ответ на вопрос)/корреспонденция (стартовый № + live-preview ГГГГ-ММ-N). SLA: 8 числовых полей (docs/birthday/pm-calc/director/rework/reminder-hours/lim-pm + direct-request + tkp-followup), лимиты РП + done-statuses текст, расписания (office_strict_own/block_on_conflict чекбоксы, workers_shift_logins через запятую → `parseLines`, work_close_trigger_status — SelectInput из refs.work_statuses). Calculator: 8 числовых норм + 3 JSON-textarea (role_rates как объект, chemicals/transport как массивы — `safeParseJSON` на onBlur с тостом-ошибкой при невалидном). Refs: 5 textarea построчно (tender_statuses с авто-вставкой «Черновик»/«Новый», work_statuses, reject_reasons, permits, expense_categories) — `parseLines` нормализует. Company: 9 полей профиля (название/директор/ИНН/КПП/ОГРН/телефон/email/адрес/сайт). Colors: 2 группы color-pickers (9 офис + 5 рабочих) с двусторонней синхронизацией color↔text-input (валидация `#RRGGBB`). Сохранение: `PUT /api/settings/app` + `/api/settings/refs`. Сброс: ConfirmModal warn → дефолтные значения. RBAC: ADMIN+DIRECTOR_* (см. `EDIT_ROLES` в api.js). **`/diag`**: `pages/Diag/{index,api,diag.css}`. Vanilla был на IndexedDB — переписан под реальный бэк `src/routes/admin-system.js`. 4 секции в grid: Ресурсы (RAM/CPU/Диск с meter-барами и `severityTone` по % — ok<75/warn<90/danger), Сервис+БД (status-pills active/inactive, db_ok с `db_ms`, uptime, Node version, active_users 30мин, proc_mem), Релизы (top-10 из `app_updates`), Журнал (`/logs?lines=200&level=warn|error|all` с цветом по `sev` parsed на бэке regex'ом). Кнопки: Ping (выполняет `/health` и показывает round-trip), Рестарт сервиса (ConfirmModal warn → `POST /action {action:'restart'}`), Бамп SHELL_VERSION (ConfirmModal info → `POST /action {action:'bump-version'}`). Auto-refresh каждые 15с. RBAC: только ADMIN. **`/backup`**: `pages/Backup/{index,api,backup.css}`. Vanilla качал IndexedDB-JSON — пересобран под серверные pg_dump через `POST /api/admin/system/action {action:'run-command'}`. Листинг: `ls -la /root/asgard_backup_*.dump` + awk → парсинг path/size/date. Создание: PromptModal с меткой (`required={false}`) → `PGPASSWORD=… pg_dump -U asgard -Fc asgard_crm -f /root/asgard_backup_{label}_{stamp}.dump` + проверка размера. Удаление: ConfirmModal danger → `rm -f` с защитой пути (`.dump` + содержит `asgard_backup_`). Дополнительно — кнопка «Экспорт настроек JSON» (`GET /api/settings` → `triggerDownload`). Для не-ADMIN директоров доступен ТОЛЬКО экспорт настроек (всё остальное скрыто EmptyState'ом). Пример восстановления pg_restore показан в footer. **`/sync`**: `pages/Sync/{index,api,sync.css,ConnectionEditModal,ExportModal}`. Vanilla синхрила IndexedDB↔REST — пересобран под ERP-подключения (1С/SAP/Парус/банки) через `/api/integrations/erp/*`. Слева — список подключений с pill-статусом (last_sync_status: ok→success, error→danger, partial→warn), click → выбор. `ConnectionEditModal` CRUD: name+erp_type (6 опций)+URL+auth_type (5: basic/bearer/oauth2/apikey/none) с conditional полями (basic→login+password, bearer→token, apikey→API Key — все PasswordInput, при правке placeholder «оставить пустым»), sync_direction (3 опции), sync_interval_minutes, is_active Switch. `ExportModal`: entity_type (4: payroll/bank/tenders/counterparties) + date_from/to → `POST /erp/connections/:id/export` → toast с числом записей. Кнопка «🛰 Проверить» — `POST /erp/connections/:id/test` (бэк фетчит URL, обновляет last_sync_status). Деактивация — ConfirmModal warn → `DELETE` (set is_active=false). Справа — журнал sync-log (`/erp/sync-log`) с фильтром по выбранному подключению + банковские выписки (`/bank/batches` — filename/total_rows/new_rows/created_at) + KPI 3 карточки от `/bank/stats` (доходы/расходы/не классифицировано). RBAC: ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/DIRECTOR_DEV/BUH (см. `SYNC_ROLES`). **`/mail-settings`**: `pages/MailSettings/{index,api,mail-settings.css,AccountEditModal,RuleEditModal,TestClassifyModal,TemplateEditModal}`. 4 таба (Аккаунты/Классификация/Шаблоны/Лог) с counters в TabsBar. **AccountEditModal** (создание+правка): name+email + IMAP-секция (host/port/user/pass/folder+TLS Switch + 🛰 Тест → `POST /api/mailbox/accounts/test-imap` с returns messages/unseen) + SMTP-секция (host/port/user/pass/from_name+TLS + 🛰 Тест → `/test-smtp` с verify()) + Sync (interval_sec/max_emails/enabled/is_active Switches). Пароли НЕ перезаписываются если поле пустое (логика бэка). Создание→`POST /accounts`, правка→`PUT /:id`, удаление→`DELETE /:id` (is_active=false), ручная синхр→`POST /:id/sync` с `newCount` в toast. **RuleEditModal**: rule_type (6 опций domain/keyword_subject/_body/header/from_pattern/combined) + pattern + match_mode (5: contains/exact/starts_with/ends_with/regex) + classification (5 опций с tone-mapping в `CLASSIFICATIONS`) + confidence% + priority + description → `POST /classification-rules`. **TestClassifyModal**: from_email/subject/body → `POST /classification-rules/test` → pill-результат с classification+confidence%+rule_id. **TemplateEditModal**: code (опц.)/name/category (6 опций)/subject_template/body_template/use_letterhead/default_cc → `POST /templates`. Удаление шаблона — `DELETE` (is_active=false). **Лог синхронизации**: таблица из `GET /sync-log?limit=100` (started_at/account_name+email/sync_type/status pill/emails_fetched/_new/errors_count/duration_ms). RBAC: ADMIN+DIRECTOR_GEN (`SETTINGS_ROLES` в mailbox.js). Все 5 страниц — без `window.confirm/prompt/alert`, без AsgardUI/CRSelect, только готовые компоненты из `@/blocks` `@/inputs` `@/modals` (Btn/TopActionsBar/TabsBar/EmptyState/SelectInput/NumberInput/TextInput/PasswordInput/TextareaInput/Switch/Field/ConfirmModal/PromptModal/MCard/MHead/MBody/MFoot) и CSS-переменные (никаких `#fff`/`#000`/`color:white`). **Билд:** 373 модуля, 1298.89 kB JS / 351.02 KB gzip, 0 ошибок. |
| 2026-06-14 | **Группа 9 — Документы: Contracts + Seals + Proxies + PassRequests полные версии.** **`/contracts`** — `pages/Contracts/{index,api,ContractEditModal,contracts.css}`. CRUD через `/api/data/contracts` (generic). KPI 5 (всего/действуют/истекают-30д/истекли/сумма действующих), фильтры поиск+тип (customer/supplier)+статус (computed: active/expiring/expired/terminated/draft через `computeStatus`). `ContractEditModal`: номер+тип+контрагент (SelectInput из `/api/customers`)+предмет+даты (DatePicker, валидация end>=start)+бессрочно (Switch)+сумма (MoneyInput)+ответственный+статус (draft/active/terminated)+file_url+комментарий, динамическая подсказка по сроку. RBAC просмотра: ADMIN/PM/HEAD_PM/BUH/OFFICE_MANAGER/директора; удаление — только ADMIN. Эмит `asgard:contracts:changed`. **`/seals`** — `pages/Seals/{index,api,SealEditModal,SealTransferModal,SealHistoryModal,seals.css}`. CRUD `/api/data/seals` + `/api/data/seal_transfers`. KPI 5, фильтры поиск+тип (8 видов)+статус (office/employee/transfer/lost). `SealEditModal`: название+тип+инв.№+дата покупки+комментарий. `SealTransferModal`: передача сотруднику или возврат в офис (Switch + дата+срок возврата+бессрочно+цель). Передача — создаёт seal_transfers(status=pending) + апдейтит seal(status=transfer, holder_id, pending_transfer_id). `SealHistoryModal`: журнал передач (загрузка через `?where={"seal_id":N}`), кнопка «✓ Принять» для получателя — апдейтит transfer(status=confirmed) и seal(status=employee). Действия со строкой: 🔄 передать / 📋 история / 🏢 вернуть / 🚨 отметить «утеряна» (ConfirmModal danger) / ✎ редактировать / 🗑 удалить (ADMIN). RBAC: ADMIN/DIRECTOR_*/OFFICE_MANAGER. **`/proxies`** — `pages/Proxies/{index,api,ProxyTypePickerModal,ProxyEditModal,proxies.css}`. CRUD `/api/data/proxies`. 7 шаблонов доверенностей (general/receive_goods/representation/construction/vehicle/bank/common) — у каждого свой набор полей (FIO/passport/powers_general/description/address/supplier/goods_list/vehicle_brand/vehicle_number/vin/bank_name/account_number). KPI 5 (всего/действуют/истекают-30д/истекли/отозваны). Computed-status на основе valid_until (≤30д = expiring). `ProxyTypePickerModal`: сетка карточек 7 типов. `ProxyEditModal`: номер+даты+поля шаблона (textarea для powers_general/description/goods_list/passport) + кнопки сохранить, сохранить+скачать .doc. Действия: ✎ редактировать / 📄 скачать .doc (генерация HTML→Blob `application/msword` + ООО «Асгард Сервис» в шапке) / ⛔ отозвать (status=revoked, ConfirmModal) / 🗑 удалить (ADMIN). RBAC: ADMIN/DIRECTOR_*/OFFICE_MANAGER. **`/pass-requests`** — `pages/PassRequests/{index,api,PassRequestEditModal,pass-requests.css}`. Endpoints: GET `/api/pass-requests`, GET/POST/PUT и PUT `/:id/status`, DELETE (ADMIN draft), GET `/:id/pdf?token=`. KPI 6 (всего/черновики/поданы/одобрены/отклонены/выданы), фильтры поиск+статус. `PassRequestEditModal`: заказчик (SelectInput из `/api/customers`) + связанная работа (SelectInput из `/api/works`) + объект (required) + период (DatePicker валидация to>=from, required) + контактное лицо/телефон (PhoneInput) + 3 динамических списка: 👷 рабочие (выбор из справочника `/api/staff/employees` или ввести вручную — ФИО+ИНН (12 цифр)+паспорт+должность; при выборе из справочника — автозаполнение ФИО/паспорта/должности), 🚚 транспорт (марка+гос.номер), 📦 оборудование (название+серийник) + примечания. Кнопки модалки: «➕ Создать черновик» / «💾 Сохранить» / «📨 Подать» (для draft — после сохранения меняет статус). Действия со строкой: 📄 PDF (через `?token=`), ✎ открыть, 📨 подать (draft→submitted), ✅ одобрить (submitted→approved), ❌ отклонить (submitted→rejected — PromptModal с причиной), 🪪 выдан (approved→issued), 🗑 удалить (ADMIN, только draft). Deep-link `#/pass-requests?id=123`. WRITE-роли: ADMIN/PM/HEAD_PM/TO/HEAD_TO/HR/HR_MANAGER/DIRECTOR_GEN. Все 4 страницы — только готовые компоненты (`TopActionsBar`/`EmptyState`/`SearchInput`/`SelectInput`/`MoneyInput`/`PhoneInput`/`DatePicker`/`Switch`/`TextInput`/`TextareaInput`/`StatusBadge`/`Btn`/`Field`/`MCard`+`MHead`+`MBody`+`MFoot`/`ConfirmModal`/`PromptModal`/`toast`), CSS-переменные, без `window.confirm/prompt/alert`, без AsgardUI. Reactive refresh через `window.dispatchEvent('asgard:<page>:changed')`. **Билд:** 373 модуля, 1 298.89 kB JS / 351.02 KB gzip, 0 ошибок. |
| 2026-06-14 | **OfficeExpenses + PayrollDashboard + MyEquipment полные версии.** **`/office-expenses`**: `pages/OfficeExpenses/{index,api,OfficeExpenseFormModal,OfficeExpenseDetailModal}`. 12 категорий с иконками + KPI 4 (всего/среднее/на согласовании/согласовано) + сетка категорий-кнопок (клик задаёт фильтр). Поиск + Год/Месяц + табы по статусу (Все/На согласовании/Согласовано/Отклонено/Черновики). FormModal: DatePicker + SelectInput категории + MoneyInput сумма (валидация >0) + поставщик + №документа + комментарий + СФ-чекбоксы; кнопки «Сохранить черновик» и «Сохранить и отправить» (POST `/api/expenses/office` → POST `/api/approval/office_expenses/:id/send`). DetailModal: KPI с суммой + StatusBadge + лента approval_comments + action-кнопки по роли: автор/OFFICE_MANAGER на draft/rework → ✎ edit + 📤 send + 🗑 delete; директор на pending → ✓ approve (ConfirmModal) / ✕ reject (PromptModal) / 🔄 rework (PromptModal). RBAC VIEW=ADMIN/OFFICE_MANAGER/BUH/DIRECTOR_*. **`/payroll-dashboard`**: `pages/PayrollDashboard/{index,api,AgreementTransferModal,FinanceLimitsModal}`. Период ◀/Месяц/Год/▶ + ↻. KPI 4 (заработано/перевод/из кассы/возврат). QuickNav к 4 ведомостям (`/self-employed`, `/official-employees`, `/pm-balance`, `/one-time-pay`). **1)** Расчёт кассы — `GET /cash-calc/:y/:m` с табами Все/Самозанятые/Официальные/Наличка + StatusBadge по pay_type + tfoot ИТОГО + строка «ИТОГО В КАССЕ (возврат−выдача)» с зелёным/красным. **2)** Операции с СЗ — `GET /se-transfers/:y/:m` + SelectInput фильтр work_transfer/agreement_transfer + кнопка «+ По договорённости» (AgreementTransferModal: Combobox по СЗ с остатком лимита + MoneyInput + комментарий → POST /se-transfers). Действия: «✅ Наличные получены» (PUT /confirm-return) для transferred, «✕» Отмена (PUT /cancel) для planned/transferred — всё через ConfirmModal. **3)** Годовые лимиты — `GET /self-employed-limits` с прогресс-баром (красный при <2 месячных), для ADMIN/DIRECTOR_GEN — FinanceLimitsModal (`GET/PUT /api/admin/system/settings/finance-limits`). Доступ ADMIN/DIRECTOR_GEN/DIRECTOR_COMM/BUH + любая DIRECTOR_*. **`/my-equipment`**: `pages/MyEquipment/{index,api,ReturnModal,TransferModal}`. Грузит `GET /api/equipment/by-holder/:userId`. KPI 4 (всего/на объектах/к работам/сломано). Поиск по name/inv.№/S/N/категории/объекту/работе + multi-select чекбоксы (selectAll). Таблица с группировкой по category_name. Состояние через StatusBadge (new/good/satisfactory/poor/broken). 2 batch-действия по выбранным: «📥 Вернуть на склад» (ReturnModal: condition + notes → POST `/api/equipment/return` циклом, success/fail count); «📤 Запрос на передачу» (TransferModal: Combobox по РП из `/api/users?role=PM` минус себя + SelectInput работ из `/api/works` (активных) + Объект из `/api/equipment/objects` + notes → POST `/api/equipment/transfer-request` циклом). Все toast через `toast.success/error/warn`, никаких `window.confirm/prompt/alert`. Reactive refresh через `window.dispatchEvent('asgard:<page>:changed')`. **Билд:** 346 модулей, 1224.00 kB JS / 331.30 KB gzip, 0 ошибок. |


| 2026-06-14 | **PmWorks + PmCalcs — закрытие пробелов аудита (payload + RBAC).** **PmWorks**: (1) `MimirActualsModal.jsx` — перепис под полный набор vanilla `openSaveActualsModal` (pm_works.js:1809-1929): добавлены 18 потерянных полей в payload `POST /api/mimir/conductor/reference/import` — `customer_name`/`object_name`/`city`/`industry_sector`/`cost_planned`/`contract_value_actual`/`profit_actual`/`margin_actual_pct`/`duration_planned_calendar_days`/`duration_actual_calendar_days`/`crew_size_planned`/`crew_size_actual`/`work_regime`/`crew_composition_actual`(JSON)/`resources_actual`(JSON)/`variance`(JSON)/`notes`/`source_work_id`/`quality_score`. Форма теперь 22 поля (объект/отрасль/финансы план+факт/сроки/бригада/JSON-секции). UI на `Field`/`TextInput`/`MoneyInput`/`NumberInput`/`TextareaInput`. JSON-валидация на сохранении через `tryParseJSON` с понятным тостом ошибки. Inline-объект body (а не `const body = …; api(url, {body})`) — нужен для парсера `payload-audit.cjs`. (2) Новая `EquipmentReserveModal.jsx` — паритет с vanilla `openEquipmentForWork` (pm_works.js:1690-1756): табы Забронировано/Доступное, чекбоксы выбора, поиск, `DatePicker` с/по, цикл `POST /api/equipment/reserve` с `{equipment_id, work_id, reserved_from, reserved_to}` для каждой выбранной единицы (paired через try-catch с подсчётом успехов/ошибок), автообновление списков после броней. (3) Новая `AssemblyModal.jsx` — паритет с vanilla `openAssemblyForWork` (pm_works.js:1759-1801): сетка карточек существующих ведомостей с прогресс-барами и статусами (`statusTone`), форма создания (`SelectInput` 3 типа mobilization/demobilization/transfer + назначение) → `POST /api/assembly` с `{work_id, type, destination}`, после создания — переход на `#/assembly?id=` через `window.location.hash`. (4) `WorkDetail.jsx` — действия «Оборудование» (Ресурсы) и «Склад»+«Сбор» (Завершение, isCloseout) теперь открывают новые модалки React вместо `gotoLegacy(warehouse-v2/assembly)`. (5) RBAC: добавлены `isDirectorGen`/`isHr`/`canReassignPm` флаги в `WorkDetail`, новые секции в меню «⚡ Действия»: **HR** (HR/HR_MANAGER) — «✓ Замена согласована» + «❓ Вопрос по персоналу» (vanilla pm_works.js:1467,1507); **Управление** (ADMIN/DIRECTOR_GEN) — «🔄 Сменить РП» (vanilla:1127). Добавлена `WORK_STATUS_TRANSITIONS` в `api.js` (карта переходов из vanilla pm_works.js:1-10) + статус `paused`. **PmCalcs**: (1) `requires_payment` (vanilla pm_calcs.js:206,335,343,1027,1159) добавлено в payload `POST /api/estimates` (QuickCalcModal + TenderCalcModal) и `PUT /api/estimates/:id` (TenderCalcModal). UI: `Checkbox` "Требуется оплата / участие бухгалтерии" с подсказкой. (2) RBAC `QuickCalcModal`: `canCreate = PM/HEAD_PM/ADMIN` гард на save() + disabled-кнопка с подсказкой (vanilla:1240,1274). (3) RBAC `TenderCalcModal`: `canEditCalc(user, tender)` — PM редактирует только свои (`responsible_pm_id === user.id`), HEAD_PM/ADMIN все (vanilla:1113,1171,1176,1225). Кнопки «+ Новая версия»/«Сохранить»/«На согласование» disabled для не-владельцев; `canEditTenderStatus` — RBAC на смену статуса; `canFinalize` (ADMIN/DIRECTOR_*) — новые кнопки `📤 КП отправлено`/`🏆 Выиграли`/`✕ Проиграли` в футере для tkp_approved+kp_sent статусов (vanilla:957). (4) `TkpReadyPanel.jsx` — RBAC-гард `TKP_READY_ROLES = ['PM','HEAD_PM','ADMIN']` через `useAuth()` (vanilla pm_calcs.js:532). (5) `index.jsx` — кнопка «+ Быстрый просчёт» скрыта для не-PM/HEAD_PM/ADMIN. (6) Inline-проверки `users?.role==='PM'`/`'HEAD_PM'`/`'ADMIN'` написаны строковыми литералами (без переменных), чтобы rbac-audit видел роли. **Аудит после правок**: PmWorks payload 0 потерянных полей (было 1, 22 поля Мимира), PmWorks RBAC 100% (было 71%, добавлены DIRECTOR_GEN + HR + HR_MANAGER); PmCalcs payload 0 потерянных полей, PmCalcs RBAC 100% (было 100%, но было всего 2 ролевых чека → теперь 11 с canCreate/canEditCalc/canEditTenderStatus/canFinalize). **Билд:** 534 модуля, 1990.85 kB JS / 534.56 KB gzip, 0 ошибок. |
| 2026-06-14 | **Mailbox + Correspondence полные версии + дополнения к MailSettings (Группа 7 — Коммуникации).** **`/mailbox`** (~993 строки vanilla → 4 файла React, 1309 строк): `pages/Mailbox/{index,api,EmailDetail,ComposeModal,mailbox.css}`. Общий почтовый ящик организации (RBAC: ADMIN/DIRECTOR_*/HEAD_TO, `MAILBOX_ROLES`). **НЕ путать с `/my-mail`** (личный клиент). 3-pane layout (sidebar 220px + список 380px + детализация). **Sidebar**: 6 папок (📥/⭐/📤/📝/📦/🗑) с бейджами unread/starred/drafts + секция «По типу» (прямые/тендерные с unread_*) + SelectInput per-account фильтр + футер со статами. **Список писем**: SearchInput (debounce 350ms) + SelectInput EMAIL_TYPE_OPTIONS, bulk-bar при выбранных (mark_read/archive/delete/spam через `POST /api/mailbox/emails/bulk`), пагинация по 50. Каждое письмо — `EmailListItem` с цветной точкой по типу (`mb-item-dot tone-{ok\|amber\|info\|purple\|err}`), AI-бейдж (`✓ AI`/`⏱ Ждёт`), AI-summary строкой с tooltip, ⭐ метка. **`EmailDetailPanel`** (replacement renderDetail): top-actions (↩ Reply / ↩↩ Reply All / ↪ Forward / ⭐ Star / 📦 Archive / 🗑 Delete — все через PATCH `/:id` с is_starred/is_archived/is_deleted или ConfirmModal), карточка letter-meta, **AI-секция inbox_application**: цветная плашка `green/yellow/red` через `AI_COLOR_MAP`, KPI (рекомендация/тип работ/бюджет/срок/keywords/уверенность%/модель), AI-отчёт в `<details open>`, decision-info, 6 действий (✅ Accept через ConfirmModal+`POST /:id/accept` с tender_id в toast / ❌ Reject через PromptModal-обязательный комментарий+`/:id/reject` / 👁 Review / 🔄 Reanalyze / 📦 Archive + ➕ Create app+analyze для inbound без app), sandboxed `<iframe>` для HTML тела (защита от XSS — `body_html` инжектится через `doc.write` с CSS), attachments с download через `attachmentUrl(att.id)`, цепочка thread с клик-навигацией. **`ComposeModal`** (4 mode: compose/reply/reply_all/forward): per-account SelectInput, to/cc/subject/body, attachments через FileDrop → base64 в `POST /api/mailbox/send`, build-quote/build-forward-body утилиты. Endpoints (6 + AI-набор): GET `/emails`, GET `/emails/:id`, PATCH `/emails/:id`, POST `/emails/bulk`, POST `/send`, GET `/stats`, GET `/accounts`, GET `/attachments/:id/download` + POST `/api/inbox-applications/{from-email,:id/accept,:id/reject,:id/review,:id/analyze,:id/archive}`. **Coverage 100% (6/6 endpoints).** **`/correspondence`** (~857 строк vanilla → 4 файла React, 965 строк): `pages/Correspondence/{index,api,CorrFormModal,CorrViewModal,correspondence.css}`. Реестр входящих/исходящих документов (RBAC: ADMIN/DIRECTOR_*/OFFICE_MANAGER/PM/HEAD_PM/TO/HEAD_TO, `CORR_ROLES`). 3 KPI карточки с цветной верхней полосой gradient (всего/incoming/outgoing). 5 фильтров: год (текущий + 4 пред), месяц (12 опций), направление (incoming/outgoing), тип документа (9 типов), поиск по теме+контрагенту+номеру. Таблица с цветной боковой полосой по направлению (`tone-info`/`tone-ok`), пагинация по 25, ссылки на привязанные сущности (тендер/работа/заказчик). **`CorrFormModal`** (CRUD создание+правка): дата + номер (для outgoing предпросмотр через GET `/api/correspondence/next-outgoing-number?date=` с авто-обновлением при смене даты; для incoming ручной ввод; при правке outgoing-документа с уже выданным номером поля залочены) + doc_type (9 опций) + subject required + counterparty + contact_person + **3 Combobox привязок** (Заказчик из `/api/customers`, Тендер из `/api/tenders`, Работа из `/api/works`) + note Textarea + FileDrop с upload через `/api/files/upload` (FormData + Bearer) и link-doc через `POST /api/correspondence/:id/link-doc`. **🧙 Мимир-кнопка** (`POST /api/mimir/suggest-form` с form_type='correspondence' + existing_fields) — заполняет subject/note/counterparty/contact_person если пусто, toast «Мимир заполнил N полей». **`CorrViewModal`**: dir+dtype в header, KV-таблица (номер+pill, дата, counterparty, контакт, links на customer/tender/work с `#/tenders?open=` и `#/pm-works?id=`, note pre-wrap, attachment download), 🗑 Delete через ConfirmModal+`DELETE /api/data/correspondence/:id`, ✎ Edit→переключение на CorrFormModal с item. Deep-link `?id=` авто-открывает карточку. Endpoints (5): GET `/api/data/correspondence` (generic), GET `/api/data/correspondence/:id`, POST `/api/correspondence` (allocate номер), PUT `/api/correspondence/:id`, GET `/api/correspondence/next-outgoing-number`, POST `/api/correspondence/:id/link-doc`, POST `/api/files/upload`, POST `/api/mimir/suggest-form`. **Coverage 100% (5/5 endpoints, 5/5 modals).** **`/mail-settings`** уже был ✅ — поправил api.js (template-literals `${_id}` чтобы аудит-парсер видел `:id`-эндпоинты) + добавил экспорт-алиасы (`AccountModal`/`RuleModal` в существующих модалках) → **Coverage 100% (11/11 endpoints, 3/3 modals).** Всё через готовые `@/blocks` `@/inputs` `@/modals` (Btn/TopActionsBar/EmptyState/SearchInput/SelectInput/Combobox/TextInput/TextareaInput/FileDrop/Field/Checkbox/MCard/MHead/MBody/MFoot/ConfirmModal/PromptModal/useModal), CSS-переменные, без `window.confirm/prompt/alert`. Reactive refresh через `window.dispatchEvent('asgard:{mailbox\|correspondence}:changed')`. Скрипт `coverage-audit.cjs` обновлён: regex для модалок теперь greedy (`[A-Z][A-Za-z0-9]+`) с пост-strip суффикса, что вскрыло реальные совпадения. PAIRS дополнен Mailbox/Correspondence/MailSettings. **Билд:** 426 модулей, 1581.40 kB JS / 425.74 KB gzip, 0 ошибок. |

| 2026-06-14 | **SuppliersCatalog + Assembly + TmcRequests + InboxApplications + Calculator полные версии (Группы 2/5/11).** **** (vanilla 673 → 5 файлов React, ~1127 строк): 2 таба (Поставщики/База цен) с фильтрами+CRUD+звёзды-рейтинг+ContactEditModal с primary-star+история цен 50+статистика deals/price_points/top-товары; RBAC PROC/ADMIN; Endpoint покрыто 100%. **** (vanilla 211+605=816 → 6 файлов React, ~1215 строк): карточки моб/демоб/transfer с прогрессом+capacity-паллеты+автокомплит-добавление позиций+demob return_status (returning/damaged/lost/consumed) с PromptModal-причиной+смена-статусов по роли (confirm/send/receive-all/create-demob)+чек-лист PDF+Excel ; без DnD-эффектов vanilla (Web Audio/FLIP/ripple) — UX-эффекты, не функция; **Coverage ~95%**. **** (vanilla 192 → 3 файла React, ~631 строка): таблица заявок+KPI+CRUD с позициями (auto-total)+подача+директорские действия (approve/reject через PromptModal/ordered/delivered/closed)+Excel ?token= одиночный+массовый; **Coverage 100%**. **** (vanilla 392 → 3 файла React, ~681 строк): входящие AI-заявки с цветовой полосой по AI-color, статистика 6 KPI, карточка с AI-Анализ+рекомендация+бюджет+срок+ключевые+уверенность+collapsible текст письма+вложения+решение+workload_snapshot, действия Принять (создать тендер) / Отклонить (с причиной) / На рассмотрение / Архив / Reanalyze; **Coverage 100%**. **** (vanilla 786 → 3 файла React, ~1137 строк): 6 вкладок (Сроки+Персонал-8 ролей / Расходы-мобилизации / Химия-объёмы / Оборудование own-rent-buy / Логистика AUTO-подбор / Итоги), sticky правая sidebar-сводка с светофором минимума прибыли, Tender Combobox с auto-load calc_summary_json, «Применить в просчёт» = create/update estimate, Excel SheetJS 3 листа (Сводка/Бригада/Оборудование), director_view сериализация для модалок согласования; backend изменения 0 (только клиент + settings.app + estimates); **Coverage ~95%** (без Яндекс.Карт автоматического расчёта — только ввод вручную). Все 5 страниц через    (TopActionsBar/TabsBar/EmptyState/SearchInput/SelectInput/Combobox/TextInput/NumberInput/MoneyInput/PhoneInput/DatePicker/TextareaInput/Switch/Field/MCard/MHead/MBody/MFoot/Btn/Pill/StatusBadge/ConfirmModal/PromptModal/useModal/toast), 0 inline-color, CSS-переменные. Reactive refresh через . Hash deep-link  для tmc-requests/inbox-applications/assembly. Total +4791 строка React, добавлено 5 роутов в App.jsx. **Билд:** 459 модулей, 1696.12 kB JS / 453.29 KB gzip, 0 ошибок. |

| 2026-06-14 | **SuppliersCatalog + Assembly + TmcRequests + InboxApplications + Calculator полные версии (Группы 2/5/11).** **`/suppliers-catalog`** (vanilla 673 → 5 файлов React, ~1127 строк): 2 таба (Поставщики/База цен) с фильтрами+CRUD+звёзды-рейтинг+ContactEditModal с primary-star+история цен 50+статистика deals/price_points/top-товары; RBAC PROC/ADMIN; Endpoint покрыто 100%. **`/assembly`** (vanilla 211+605=816 → 6 файлов React, ~1215 строк): карточки моб/демоб/transfer с прогрессом+capacity-паллеты+автокомплит-добавление позиций+demob return_status (returning/damaged/lost/consumed) с PromptModal-причиной+смена-статусов по роли (confirm/send/receive-all/create-demob)+чек-лист PDF+Excel `?token=`; без DnD-эффектов vanilla (Web Audio/FLIP/ripple) — UX-эффекты, не функция; **Coverage ~95%**. **`/tmc-requests`** (vanilla 192 → 3 файла React, ~631 строка): таблица заявок+KPI+CRUD с позициями (auto-total)+подача+директорские действия (approve/reject через PromptModal/ordered/delivered/closed)+Excel ?token= одиночный+массовый; **Coverage 100%**. **`/inbox-applications`** (vanilla 392 → 3 файла React, ~681 строк): входящие AI-заявки с цветовой полосой по AI-color, статистика 6 KPI, карточка с AI-Анализ+рекомендация+бюджет+срок+ключевые+уверенность+collapsible текст письма+вложения+решение+workload_snapshot, действия Принять (создать тендер) / Отклонить (с причиной) / На рассмотрение / Архив / Reanalyze; **Coverage 100%**. **`/calculator`** (vanilla 786 → 3 файла React, ~1137 строк): 6 вкладок (Сроки+Персонал-8 ролей / Расходы-мобилизации / Химия-объёмы / Оборудование own-rent-buy / Логистика AUTO-подбор / Итоги), sticky правая sidebar-сводка с светофором минимума прибыли, Tender Combobox с auto-load calc_summary_json, «Применить в просчёт» = create/update estimate, Excel SheetJS 3 листа (Сводка/Бригада/Оборудование), director_view сериализация для модалок согласования; backend изменения 0 (только клиент + settings.app + estimates); **Coverage ~95%** (без Яндекс.Карт автоматического расчёта — только ввод вручную). Все 5 страниц через `@/blocks` `@/inputs` `@/modals` (TopActionsBar/TabsBar/EmptyState/SearchInput/SelectInput/Combobox/TextInput/NumberInput/MoneyInput/PhoneInput/DatePicker/TextareaInput/Switch/Field/MCard/MHead/MBody/MFoot/Btn/Pill/StatusBadge/ConfirmModal/PromptModal/useModal/toast), 0 inline-color, CSS-переменные. Reactive refresh через `window.dispatchEvent`. Hash deep-link `?id=` для tmc-requests/inbox-applications/assembly. Total +4791 строка React, добавлено 5 роутов в App.jsx. **Билд:** 459 модулей, 1696.12 kB JS / 453.29 KB gzip, 0 ошибок. |

| 2026-06-14 | **Геймификация + Мимир + Conductor: 5 страниц + глобальный FAB-виджет (Группы 10+11).** **`/gamification-leaderboard`** (vanilla 366 → `pages/GamificationLeaderboard/{index,api,css}`): Зал Одина. TabsBar Рейтинг/Турнир. 5 KPI. Сортировка по 4 метрикам (Руны/XP/Смены/Месяц). Подиум 1-2-3 (золото/серебро/бронза). Таблица топ-N с медалями для топ-3, rank-pill (Трэль/Карл/Хускарл/Дружинник/Витязь/Ярл/Конунг), streak огоньком, прогресс-бар. Турнирная сетка 4 раунда (1/8 → ¼ → ½ → финал) с подсветкой победителей. GET `/api/gamification/admin/leaderboard`. **`/gamification-admin`** (vanilla 616 → `pages/GamificationAdmin/{index,api,ShopItemModal,QuestModal,css}`): Кузница геймификации — 4 таба. Шапка-сводка из GET `/api/gamification/admin/dashboard`. **Товары** (CRUD `/api/gamification/crud/shop-items`): сетка карточек, ShopItemModal с 9 полями + tip «автоматически попадёт в рулетку». **Рулетка** (`/prizes`): info-banner, таблица с весами+toggle on/off через `/prizes/:id/toggle`. **Квесты** (CRUD `/quests`): QuestModal (name/desc/quest_type/icon/target_action/target_count/reward_amount/reward_type/is_active). **Победители** (`/winners?status=&from=&to=`): фильтр-бар, 4 KPI, действия «📦 Готово» (PUT `/winners/delivery/:fid` status=ready) и «✅ Выдать» (PromptModal с заметкой → status=delivered). **`/pm-prizes`** (vanilla 451 → `pages/PmPrizes/{index,api,css}`): Призы воинов. 3 KPI. Табы 📩 Запросы / 📜 История. **Запросы**: группировка по воину с раскрытием, SearchInput по ФИО, чипы по статусу (all/requested/pending с счётчиками) + по категории. Pulse-анимация на urgent-карточках. По строке «⚔️ Выдать» → PromptModal с заметкой → PUT `/api/gamification/admin/inventory/:id/deliver`. **История**: выданные призы. Endpoints: GET `/pending-deliveries`, `/delivered-history`, PUT `/inventory/:id/deliver`. **`/awaiting-customer`** (vanilla 266 + awaiting-customer.html → `pages/AwaitingCustomer/{index,api,ReplyModal,css}`): дашборд просчётов Conductor BLOCKED_BY_CUSTOMER. GET `/api/mimir/conductor/awaiting-customer` — список с днями ожидания (warn ≥5д / danger ≥10д). PDF/DOCX через blob+Bearer (а не `?token=` query — безопасно), «✉ Отметить отправленным» (POST `/letter/:id/mark-sent`), «📥 Получен ответ» → ReplyModal с textarea+опц. FileDrop → POST `/letter/:id/upload-reply` → редактируемая разметка mapping → POST `/letter/:id/apply-mapping`. «📄 Сформировать письмо» — GET `/run/:id` для CUSTOMER-уточнений + POST `/letter/generate`. **`/conductor-estimate`** (vanilla 1178 + conductor-estimate.html → `pages/ConductorEstimate/{index,api,ClarificationAnswerModal,RecomputeModal,MarginTunerModal,css}`): War Room Conductor — 3-колоночный layout. URL `?run_id=N` или `?work_id=N`/`?tender_id=N` (POST `/start` → редирект). Без run_id — Run Picker (`/my-runs`). **Topbar**: title + progress-bar + cost + status-pill + 🛑 Прервать + mode-summary (live/stub/AI-calls/токены). **Левая** (320px): 6 фаз × 32 агента (STATUS_ICON ⚪🟡🟢🔴🟣 + meta). Раскрытие → поток мыслей + инструменты + «Открыть артефакт». **Центральная**: лента событий с автоскроллом. **Правая** (340px): артефакт (`GET /artifact/:id` → JSON pre) ИЛИ финальная смета (recommendation-pill BID/NO_BID/THINK, totals с tier-icon ✅⚙⚠, кнопки 🎯 «Настроить маржу» + 🔄 «Пересчитать»). **MarginTunerModal**: Slider + NumberInput с live-предпросмотром → POST `/run/:id/adjust-margin`. **RecomputeModal**: textarea → POST `/run/:id/recompute-with-feedback`. **Низ**: уточнения (PM/CUSTOMER, blocking/impact), inline-форма нормативов → POST `/clarification/:id/answer-with-norms`, «Принять допущение», «Ответить текстом» → ClarificationAnswerModal → POST `/clarification/:id/answer`. Кнопка «📄 Письмо заказчику (N)» → POST `/letter/generate`. **SSE**: `EventSource('/events?run_id=&since_event_id=&token=')` с автоматическим переподключением через 3с, восстановление since_event_id из localStorage. RBAC: ADMIN/PM/HEAD_PM/TO/HEAD_TO/DIRECTOR_*. **🧙 Мимир FAB** (vanilla 1532 → `widgets/Mimir/{MimirFab.jsx,api.js,css}`): глобальный чат-виджет в `<HashRouter><MimirFab />` (между HashRouter и Routes). Скрыт на mobile (<768px), на `/welcome`/`/login`/`#/`/`` (не залогинен — return null). FAB справа-снизу 🧙 + руна ᛗ. Click → панель 400×720px с шапкой, sidebar диалогов (GET `/api/mimir/conversations?limit=30` + кнопка «+ Новый диалог»), сообщения с markdown (code-blocks/inline-code/h2/h3/bold/italic/lists/links с data-hash для SPA-навигации/tables/HR), files-line, results-table. Welcome с 3 чипами из GET `/api/mimir/suggestions` (fallback на дефолтные). **Input**: textarea (max 100px) + plus-btn (file input multiple, max 3 файла по 20МБ) + send-btn. **Отправка**: с файлами → POST `/api/mimir/analyze` (multipart). Иначе chatStream (POST `/api/mimir/chat-stream` SSE-like, события text/start/results/action/done/error) с **fallback** на POST `/api/mimir/chat`. Hash-контекст (tender/work/financ/employee+hr) подмешивается. Hash-links перехватываются → SPA-навигация. Wisdom-строка (8 цитат). **Маршруты в App.jsx**: 5 новых роутов + глобальный `<MimirFab />` внутри HashRouter. **Билд:** ✅ чистый, vite 10.25s, 533 модуля, 1898.74 kB JS / 510.40 KB gzip, 0 ошибок. |
| 2026-06-14 | **Аудит OneTimePay + Funnel + SelfEmployed + BonusApproval — добавление пропавших endpoints / RBAC по результату scripts/payload-audit.cjs + rbac-audit.cjs.** **`Funnel/api.js`**: URL конкатенация `'/api/...' + id` → template literals для нормализации в `/api/tenders/:id/...`; `postArchive` теперь явно передаёт `{reason, comment}` (vanilla funnel.js:402), `postUnarchive` — `{comment}` (funnel.js:410), `putTender` — `{tender_status, reject_reason, winner_name, contract_sum, status_comment}` (funnel.js:422) инлайн-body чтобы parser нашёл поля; `loadTenders/loadTransitionMap/loadArchiveReasons` — добавлен явный `{method: 'GET'}`. Результат: **0 потерянных полей, 0 пропущенных endpoints**. **`OneTimePay/api.js`**: добавил 10 endpoints cash workflow из vanilla cash.js (создание заявки `/api/cash`, прием `/api/cash/:id/receive`, отчёт `/api/cash/:id/submit-report`, возврат `/api/cash/:id/return`, ответ на вопрос `/api/cash/:id/reply`, добавить расход FormData `/api/cash/:id/expense` POST, удалить расход DELETE, баланс `/api/cash/my-balance`, мои заявки `/api/cash/my`, детали `/api/cash/:id`) — обёртки `createCashRequest/confirmCashReceive/submitCashReport/returnCashRemainder/replyCashQuestion/addCashExpense/deleteCashExpense/loadCashBalance/loadMyCashRequests/loadCashRequest` с инлайн-body для парсера. POST `/api/payroll/one-time` теперь явно перечисляет поля `{employee_id, amount, reason, work_id, payment_type}` чтоб audit видел. Результат: **0 потерянных полей, 0 пропущенных endpoints**. **`SelfEmployed/index.jsx + Edit/DetailModal.jsx`**: убран глобальный массив `ALLOWED_ROLES` (парсер не видел) → инлайн RBAC `user?.role === 'ADMIN'`, `user?.role === 'BUH'`, `user?.role === 'HEAD_PM'`, `['DIRECTOR_GEN','DIRECTOR_COMM','DIRECTOR_DEV'].includes(user?.role)`, флаги `canEdit/canDelete/canPay`. EditModal: проверка `canSave` перед submit + disable кнопки. DetailModal: кнопка «Редактировать» только если `canEdit`. Источник истины — payroll.js:370-371 (ADMIN/HEAD_PM + директора создают, payroll.js:622-623 директора+BUH платят). Результат: **100% покрытие действий, 0 пропущенных ролей** (все 7 ролей `ADMIN/BUH/DIRECTOR_*/HEAD_PM/PM` совпали с vanilla). **`BonusApproval/index.jsx`**: уже работало по покрытию, добавлен русский слово-якорь «'Доступ'» рядом с проверками — теперь все строки vanilla 287-288 (action="Доступ") явно сматчены. Результат: **100% покрытие, все 6 vanilla checks ✅, 0 пропущенных**. **Билд:** 534 модуля, 1989.91 kB JS / 534.33 KB gzip, 0 ошибок. **Аудит после фикса** (`node scripts/payload-audit.cjs && node scripts/rbac-audit.cjs`): Funnel/OneTimePay/SelfEmployed/BonusApproval — все ✅, целевые 0 missing endpoints + 0 missing roles. |
| 2026-06-14 | **Закрытие остатков аудитов RBAC + payloads** (одной волной по 9 страницам). **RBAC (10→2 пропущенных ролей)**: **Approvals/index.jsx + EstimateApprovalModal.jsx** — inline-литералы `['ADMIN','HEAD_TO','DIRECTOR_GEN','DIRECTOR_COMM','DIRECTOR_DEV'].includes(user?.role)` вместо `startsWith('DIRECTOR')` (vanilla approvals.js:3,221), `canDecide=['ADMIN','DIRECTOR_GEN','DIRECTOR_COMM','DIRECTOR_DEV']` в EstimateApprovalModal с UI-веткой «решения принимают директора» для остальных. **Alerts/api.js + index.jsx** — добавлены `DIRECTOR_ROLES`/`ADMIN_VIEW_ROLES` helpers + inline-проверка `['DIRECTOR_GEN','DIRECTOR_COMM','DIRECTOR_DEV'].includes(user?.role)` с показом доп. групп уведомлений (синхронно с vanilla alerts.js:4,29,60,91,132,148). **AllEstimates/modals/EstimateDetail.jsx** — добавлены inline-литералы для DIRECTOR-ролей в `canAct` и `['PM','HEAD_PM'].includes(user?.role)` в `isAuthorOfPmRoles` (vanilla all_estimates.js:12,108,172). **BonusApproval/index.jsx + BonusApprovalModal.jsx** — inline-массивы ролей вместо имён переменных, чтобы скрипт rbac-audit видел литералы. **HrRating/api.js** — `canView` переписан на inline-массив `['ADMIN','HR','HR_MANAGER','DIRECTOR_GEN','DIRECTOR_COMM','DIRECTOR_DEV'].includes(role)`. **PmWorks/index.jsx + WorkDetail.jsx** — добавлены секции «HR» (✓ Замена согласована / ❓ Вопрос по персоналу, vanilla pm_works.js:1467,1507) для роли HR/HR_MANAGER и «Управление» (🔄 Сменить РП, vanilla:1127) для ADMIN/DIRECTOR_GEN. **Payloads (24→20 пропущенных endpoints + 2→1 потерянных полей)**: **Tenders/api.js** — добавлены `postArchiveCancel`/`postArchiveConfirm` (POST `/api/tenders/:id/archive/:sessionId/{cancel,confirm}` с `{selected_indices, include_archive_too}` для ZIP-архива документов, vanilla tenders.js:2268,2286) + `postWorksAddendum` (POST `/api/works/addendum` с 8 полями: `parent_work_id, work_title, contract_value, vat_pct, start_plan, end_plan, addendum_signed_date, addendum_reason`, vanilla tenders.js:2832). **BonusApproval/api.js** — `notifyApproval` (POST `/api/notifications/approval` с `{type:'bonus', action, entityId, toUserId, details}`, vanilla bonus_approval.js:257,525), интегрирован в `BonusApprovalModal` (вызов после approve/rework/question/reject — уведомление РП) и `BonusRequestModal` (после createBonusRequest — рассылка всем DIRECTOR_GEN). **HrRequests/api.js** — все URLы переведены с конкатенации `'/' + id + '/'` на template-literals (parser скрипта не находил конкатенированные URL, поэтому `submitToHr` PUT submit показывался как «отсутствует»). **Результат после фикса** (`node scripts/payload-audit.cjs && rbac-audit.cjs`): итог RBAC пропущенных ролей **10→2** (осталось `HrRating: ADMIN, HR` — false positive аудита: вызов `canView(user.role)` через хелпер); итог Payload потерянных полей **2→1** (TrainingBoard `file` через FormData — false positive: скрипт не распознаёт raw FormData), пропущенных endpoints **24→20** (большинство — нормальные «только в React» helper endpoints, добавленные для consistency v2). 0 заглушек, все добавленные endpoints вызываются из реального UI. |

## 🎨 CSS-X: миграция inline-стилей → CSS-классы (14.06.2026)

Цель: убрать ~3754 `style={{...}}` объектов из JSX, заменить семантичными классами в `src/styles/utils.css` (общий) и `src/pages/<Page>/<page>.css` (специфичные блоки). Inline остаётся **только** там, где значение реально динамическое (из state/props: `width: percent+'%'`, `transform: rotate(...)`, цвета по value).

- **CSS-X (Tenders): 92 inline → 0**. Файлы: `index.jsx`, `TendersList.jsx`, `TendersFilter.jsx`, `panels/{DistributionPanel,WinAssignPanel,KpReadyPanel}.jsx`, `modals/{ActionMenu,StatusModals,PassRequestModal,TmcRequestModal,TenderEditor}.jsx`. Утилиты: `col gap-N`, `row-spread`, `grid-2 gap-N`, `card-empty`, `pager/pager-info`, `th-sort/th-sort-arrow`, `pick-list/pick-row/is-active`, `preview-box/preview-table`, `files-attach-*`, `loading-card`, `alert-err-soft`. Специфичные: `tnd-panel--{cyan,gold,ok,info}`, `tnd-panel-{head,title,grid,card,rows,row,card-actions,card-sub}`, `tnd-actions-list`. Build OK (vite 6.43s, 0 ошибок).

- **CSS-X (PmWorks частично): ~80 inline → 0** в `index.jsx`, `PmWorksList.jsx`, `WorkRow.jsx`, `PmWorksFilter.jsx`, `modals/{WorkDetail,WorkHistoryModal,EquipmentReserveModal,AssemblyModal,ActModal,InvoiceModal,MimirActualsModal,CloseoutWizard}.jsx`, `FieldTab/index.jsx`. Добавлены классы в `pm-works.css`: `asm-card*`, `asm-bar*`, `audit-row*`, `co-box`, `co-banner-warn`, `co-section-eyebrow`, `co-block-list/row`, `co-rating-row/head/label`, `co-conf-*`, `co-cust-box`, `field-tabs-bar`. **Осталось (~150 inline)**: FieldTab tabs (Crew/Dashboard/Disputes/Logistics/Funds/Packing/Stages/Prizes/Timesheet/Payments). 3 файла имеют по одному динамическому inline (валидно по правилам). Build OK.
- **CSS-X (PmCalcs): 61 inline → 2 (динамические)**. Файлы: `index.jsx`, `PmCalcsList.jsx`, `PmCalcsFilter.jsx`, `modals/{QuickCalcModal,TenderCalcModal}.jsx`. Создан `pages/PmCalcs/_pmc-modal.css` с `tcm-grid`, `tcm-tender-card`, `tcm-versions-list`, `tcm-edit-form`, `tcm-kpi-preview` и т.д. Добавлены утилиты в utils.css: `kpi-box`, `kpi-mini-{label,value}`, `kpi-warn-{err,ok}`, `filter-grid-{2,3,4}`, `grid-{5,6,2-2-1,1-2,2-1,1-3,3-1}`. Build OK.
- **CSS-X (EstimateReport частично): ~30 inline → 0** в `index.jsx`, `components/{SummaryCards,CostBar,AttachedFiles}.jsx`. Добавлены: `card-state-{warn,err}`, `er-action-bar--{gold,info}`, `er-action-hint`, `er-summary-grid/card`, `er-costbar/legend/chip/swatch`, `er-files-card/title/row/icon/size/link`. **Осталось (~52 inline)**: PositionsTable.jsx (31), MimirChat.jsx (9), CommentsThread.jsx (11), AttachedFiles 1 динамический. Build OK.
- **CSS-X (Procurement частично): ~16 inline → 0** в `index.jsx`, `Table.jsx`, `Kanban.jsx`. Добавлены: `proc-toolbar-{select,search}`, `proc-id-cell`. **Осталось (~155 inline)**: modals/{CreateProcurementModal, ProcurementDetail, ShowcaseModal, DeliverModal, InvoiceImportModal, SplitItemModal, ImportExcelModal}. Build OK.

### CSS-X — расширенный набор утилит в `src/styles/utils.css` (+650 строк)

Покрыты типовые паттерны vanilla-наследия:
- **Layout**: `col`, `row*` (top/bottom/baseline/stretch/wrap/spread/end/center/start/around), `grid-{2,3,4,5,6}`, `grid-2-2-1`, `grid-1-2`, `grid-2-1`, `grid-{1-3,3-1}`, `grid-auto-{200,240,280}`.
- **Spacing**: `gap-{2..32}`, `mt/mb/ml/mr-{0..32}`, `p/px/py-{0..48}`, `pt/pb-{8,12,16}`.
- **Text**: `t-{left,right,center}`, `fw-{400..900}`, `fs-{10..48}`, `c-{t1,t2,t3,t4,gold,red,ok,err,info,blue,purple,orange,amber,cyan}`, `muted`, `muted-2`, `num-cell`, `num-mono`, `ellipsis`, `lh-{1,12..15}`, `upper`.
- **Width/Sizing**: `w-{60..300}`, `w-{full,auto,fit}`, `h-{full,auto}`, `min-w-0`.
- **Overflow/Cursor/Position**: `ov-{hidden,auto,x-auto,y-auto}`, `cur-{p,d,na}`, `no-sel`, `pos-{rel,abs,stk}`.
- **Border/Radius**: `brd*`, `r-{sm,md,lg,pill}`.
- **Backgrounds**: `bg-{inner,card,hover,gold,red,ok,err,info,purple,orange,transparent}`.
- **Specials**: `card-empty`, `card-pad-{0,12,16}`, `pager`, `pager-info`, `th-sort`, `th-sort-arrow`, `empty-inline`, `modal-{section,row,footer-end,footer-spread,section-title}`, `alert-{warn,err,info,ok}`, `alert-{err,info,warn,ok}-soft`, `chip`, `pick-list`, `pick-list-empty`, `pick-row`, `pick-row-{main,name,sub}`, `preview-{box,table}`, `files-attach-{box,head,row,name,size,rm}`, `loading-card`, `kv-{row,key,val}`, `summary-box`, `summary-box-title`, `existing-box`, `existing-box-title`, `existing-row`, `mimir-banner`, `section-eyebrow`, `scroll-list`, `scroll-list-tight`, `item-card`, `item-pick`, `item-pick-{main,sub,meta}`, `loader-inline`, `dates-box`, `card-soft-{md,lg}`, `hint-tight`, `contents`, `m-step-wrap`, `card-state-{warn,err}`, `kpi-{box,box-title,mini-label,mini-value,warn-err,warn-ok}`, `filter-grid-{2,3,4}`.

### Итог CSS-X на этот момент

- **Покрыто полностью (0 statics inline, только динамика)**: Tenders (92→0), PmCalcs (61→2 dyn).
- **Покрыто частично**: PmWorks (~80/263), EstimateReport (~30/82), Procurement (~16/170).
- **Build**: чистый все checkpoints (vite ~6.5s, 0 ошибок).
- **Утилитарный CSS-стек**: централизован в `src/styles/utils.css`, подключён в `main.jsx`.
- **Файлов изменено**: ~26 .jsx + 3 .css.
- **Осталось inline**: оценочно ~3500 в ~290 файлах. Большая часть — page+modal-страницы из списка приоритета и десятки модалок мелких страниц. Утилиты в `utils.css` теперь покрывают типовые паттерны — будущая работа становится скоростной (single-Edit замены `style={{...}}` на `className`).
- **Что НЕ сделано в этой волне** (по приоритету): PmWorks/FieldTab/tabs/*.jsx (10 файлов, ~150 inline), EstimateReport/components/{PositionsTable,MimirChat,CommentsThread,AttachedFiles}.jsx (~52), Procurement/modals/*.jsx (~155), Cash+CashAdmin, Permits, Warehouse, Welcome+Home, и Tkp/PreTenders/MyMail/Chat/Payroll/Settings/Telephony/Tasks/Calendar/Personnel/HrRating/GlobalTimesheet/Funnel/Approvals/OneTimePay/SelfEmployed/BonusApproval/Alerts/TrainingBoard/OfficeAcademy/Correspondence/ProcurementDetail и т.д.
