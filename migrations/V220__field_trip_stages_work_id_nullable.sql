-- ═══════════════════════════════════════════════════════════════════════════
-- V220: field_trip_stages.work_id → NULLABLE
-- Контекст: общий Табель (/global-timesheet) даёт кладовщику ставить
-- отметки «склад» / «медосмотр» рабочим, у которых СЕЙЧАС нет назначения
-- на конкретную работу (заехал на склад на день; направили на медосмотр).
-- Такие этапы пишутся в field_trip_stages с work_id=NULL — но прод-схема
-- держит NOT NULL constraint, что блокирует кейс.
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE field_trip_stages
  ALTER COLUMN work_id DROP NOT NULL;
