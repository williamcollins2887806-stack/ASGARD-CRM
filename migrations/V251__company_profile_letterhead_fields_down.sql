-- V251 DOWN: содержательного отката НЕТ.
--
-- Откатывать V251 нельзя, потому что settings.company_profile — единая jsonb-карта,
-- в которой админ мог уже отредактировать поля (название банка, ОГРН, телефон, имя
-- директора) ПОСЛЕ применения V251. Удаление добавленных ключей через jsonb '-' оператор
-- стёрло бы все правки — и реквизиты компании пришлось бы вводить вручную снова.
--
-- Если откат действительно нужен (катастрофическая порча данных) — действовать
-- руками: подготовить и применить точечный UPDATE settings SET value_json = '...'
-- с заранее снятого бэкапа `settings`, либо восстановить settings из pg_dump.
--
-- letter_kinds (вторая строка settings, добавленная V251) тоже не удаляется
-- автоматически: словарь типов писем может быть отредактирован администратором
-- из UI без деплоя, и его восстановление из миграции уничтожит правки.
--
-- См. _LETTER_CONTRACT.md §2 (V251 down) и _DECISIONS.md #1, #3, #4.
--
-- Этот файл присутствует как маркер — миграционный фреймворк ожидает _down.sql
-- для каждой V*.sql и без него теряет идемпотентность. Файл намеренно пустой.

SELECT 'V251 down is intentionally a no-op — see comments above.' AS notice;
