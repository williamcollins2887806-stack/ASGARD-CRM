-- ═══════════════════════════════════════════════════════════════════════════
-- V234: employees.position / role_tag — нормализация под витрину «слесарь/мастер»
-- Источник: TIMESHEET_V2_CONTRACT.md, секция «employees.role_tag — нормализация».
--
-- ⚠️  PLACEHOLDER. Реальный data-migration mapping ЕЩЁ НЕ ПРИМЕНЯЕТСЯ в этой
--     миграции. Сейчас тут только заголовок и явный no-op, чтобы:
--       1) занять номер V234 — следующие миграции этого спринта были V235+;
--       2) `node migrations/run.js` не падал на отсутствующем файле и
--          записал V234 в таблицу migrations как «применённую»;
--       3) запись `_down` была согласована.
--
-- ──────────── ЧТО ДОЛЖНО ПРОИЗОЙТИ ПОСЛЕ ПОДТВЕРЖДЕНИЯ ПОЛЬЗОВАТЕЛЕМ ────────
-- 1. Снять с прода СВЕЖИЙ срез распределения позиций:
--      SELECT lower(position) AS pos, COUNT(*)
--      FROM employees
--      WHERE is_active = true
--      GROUP BY 1 ORDER BY 2 DESC;
--    Параллельно — срез по employee_assignments.field_role и employees.role_tag.
--
-- 2. Построить mapping pos → ('слесарь' | 'мастер' | NULL):
--      • 'слесарь' — позиции содержащие «слесарь», «монтажник», «чистильщик»,
--        «новичок», «рабочий» (без признака мастера);
--      • 'мастер' — позиции содержащие «мастер», «бригадир», «руководитель
--        смены», «старший»;
--      • NULL — административные роли (PM/ADMIN/TO/HR/BUH/...), не участвуют
--        в табеле как «дни рабочих» — для них витрина «свои дни» пустая.
--
-- 3. Применить нормализацию ТОЛЬКО к employees, у которых role_tag НЕ совпадает
--    с системной ролью (не PM/HEAD_PM/ADMIN/DIRECTOR_*/TO/HEAD_TO/WAREHOUSE/
--    OFFICE_MANAGER/PROC/BUH/HR/HR_MANAGER/CHIEF_ENGINEER). Системные роли не
--    трогать — они означают «доступ в CRM», а не «позицию на объекте».
--
-- 4. Хранить нормализованную позицию рекомендую в НОВОЙ колонке
--    employees.position_tag (VARCHAR 20) — НЕ перезаписывая исходное
--    employees.position (там историческая запись «как звали в трудовом»).
--    Маппер на бэке будет читать position_tag, фолбэк = парсинг position.
--
-- 5. Опционально добавить CHECK position_tag IN ('слесарь','мастер') либо
--    оставить свободным и валидировать на API-уровне.
--
-- 6. Полевые роли (employee_assignments.field_role: worker/shift_master/
--    senior_master) НЕ переименовываем — это другая ось (роль НА КОНКРЕТНОЙ
--    работе), а position_tag — статичная позиция сотрудника.
--
-- TODO(после подтверждения): применить mapping и записать его в этот файл
-- перед миграцией данных. См. TIMESHEET_V2_CONTRACT.md.
-- ═══════════════════════════════════════════════════════════════════════════

-- No-op: SELECT 1 чтобы run.js не падал на пустом SQL.
SELECT 1;
